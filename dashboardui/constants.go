package dashboardui

const (
	badgeNeutral = "badge badge-neutral"
	badgeWarn    = "badge badge-warn"
	badgeOK      = "badge badge-ok"
	badgeErr     = "badge badge-err"
)

const (
	statusRunning = "running"
	statusFailed  = "failed"
)

const (
	jsonKeyStatus = "status"
	jsonKeyReady  = "ready"
)
