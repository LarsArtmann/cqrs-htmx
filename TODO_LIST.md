# TODO List — cqrs-htmx

> Short-term, actionable, bounded work. Open items only.
> Completed work lives in [CHANGELOG.md](CHANGELOG.md). Long-term vision, v5 plans, and rejected ideas live in [ROADMAP.md](ROADMAP.md).

**Updated:** 2026-09-09 | **Version:** v4.9.0 family train (13 tags, 2026-09-01) + `setup/v4.10.0` (2026-09-07: DataStar dual-frontend ADR-0050, stable `RunWithAppkit`; setup v4.8.1/v4.8.2 retraction live) + go-cqrs-lite 2026-09-08-train bump (2026-09-09: command v4.10.0, event v4.11.0, id v4.6.0, query v4.8.0; `check-version-drift --strict` GREEN first time since the v1.14.0/v0.5.0 waves; all gates green) | Upstream ahead: ~122 advisory train-lag entries (templ-components v1.16.0, storage v4.9.0, system v4.7.0, ...) — next-train sweep tracked in P1 | Prior: v4.8.x era (10-tag v4.8.0 train 2026-08-14; alignment train 2026-08-30 with totp/oauth2 v4.8.1 + setup v4.8.3 + dashboardui v4.8.2, all 5 DEV-only replaces stripped; async projection startup + ADR-0048, ActorID consolidation/ADR-0111, ADR-0114 tombstone migration, MySQL completion, security-middleware consolidation, httputil v0.11.0, systemadapter module, health/v4 + auditlog/v4 bridges, composability rounds 2-3, Go 1.26.7), v4.7.0 (2026-08-07) | **Modules:** 27 in `go.work` (15 production + 11 examples + e2e/server) | **`*Service` methods:** 73 (leading v5 indicator; see ROADMAP) | **Coverage (all 15 gates green; spot-verified 2026-09-07):** root 93.2%/90, setup 86.3%/80, datastar 97.4%/90 | **Lint:** 0 issues / 15 modules (2026-09-07; golangci v2.12.2 re-verified 2026-09-01) | **appkit wave SHIPPED:** go-appkit v0.4.0 published, `RunWithAppkit` stable since 2026-09-07 (ADR-001 default-flip remains, see P3)

## Status Legend

- [ ] **OPEN** — actionable, not yet started.
- [~] **PARTIALLY DONE** — started but incomplete.

> No `[x]` items here. When a task finishes, it moves to [CHANGELOG.md](CHANGELOG.md) and is removed from this list. Deferred/rejected ideas move to [ROADMAP.md](ROADMAP.md) → "Not Planned".

---

## P1 — High impact (release follow-through & doc health)

- [ ] **Flip `check-release-train` CI step to BLOCKING (`--strict-lag 0`, drop `\|\| true`) — OVERDUE.** The gate was made advisory 2026-08-30 with a documented trigger: "flip to blocking after one full green advisory week (week began 2026-08-30)". That week ended ~2026-09-06; `.github/workflows/ci.yml:584` still reads `run: bash scripts/check-release-train.sh || true` (release-train AND version-drift steps both advisory). Repo is CONFIRMED PUBLIC, so anonymous ls-remote works on runners. Also flip `check-version-drift --strict` at the same time (it runs in `nix run .#check-modules` locally). Mind the tag-cache TTL gotcha (re-run `-- --refresh-cache` before believing a fresh UNPUBLISHED finding). Sources: docs/status/archived/2026-08-29_23-05 §c.4, 2026-08-30_11-24 §f.1, 2026-08-30_21-00 M10, 2026-09-09 report §f.38.
- [ ] **Fix systemadapter `ExternalAccountLink` unlink bug (wrong read result).** `updateExternalAccountUnlinked` (systemadapter/declarations.go:426-470) implements unlink as an update-to-zero-value fold, so `FindUserByExternalAccount` can return a zero-value entry → wrong/no user after an external account is unlinked. Fix: proper remove-from-slice fold (or a delete declaration if metaengine supports it) + regression test. Surfaced 2026-08-09 (docs/status/archived/2026-08-09_13-51 §remaining 8), still live 2026-09-09.
- [ ] **Next-train alignment sweep (~122 advisory lag entries).** Same mechanical recipe as the 2026-09-09 bump sweep: templ-components family v1.14.0→v1.16.0, go-cqrs-lite decider v4.6.0, stack v4.4.0, storage v4.9.0, watermill v4.6.0, kv v4.3.0, listing v4.4.0, scheduling v4.4.0, system v4.7.0, middleware v4.6.0, scenario/catalog/encryption/signing/prometheus/commandlifecycle/sqliteengine minors. Bundle with the CHANGELOG straddle cleanup (move `[Unreleased]` DataStar bullets under version headings — setup half already ships in v4.10.0). Train timing is an owner call (see ROADMAP Open Questions). Source: docs/status/2026-09-09 §b1/§c1/§f3.
- [ ] **Run the post-bump deferred verification pass.** The 2026-09-09 bump swapped dispatchers/stores inside measured serve paths but skipped: `test-race`, `test-fuzz`, `test-flake`, e2e (`PLAYWRIGHT_BROWSERS_PATH=/tmp/pw-browsers`), `coverage-gate`, `bench-spike` (idle machine only — see P3 bench item), `check-cqrs-lint` (also act on V006/stale-suppression findings post-bump — root go.mod's V006 rationale predates record v4.5.0). Source: docs/status/2026-09-09 §b5/§c4/§c5/§f1/§f4/§f5.
- [ ] **systemadapter first tag + final replace strip (blocked on ONE upstream tag).** UNLOCKED 2026-08-30: go-cqrs-lite published `metaengine/v4 v4.12.0`, `metaengine/sqliteengine/v4 v4.2.0`, `event v4.9.0`, `metadata v4.6.0`, `record v4.4.0`, `commandlifecycle v4.0.1` — the 5 drift axes closed repo-side and systemadapter dropped its sqliteengine + metaengine replaces (build/vet/test green hermetically). REMAINING: `metaengine/projectionadapter/v4 v4.5.0+` (v4.4.1 is the published max; `OccurredAt` on EventWithID needs v4.5.0) → then strip the last replace (systemadapter + examples/system-demo), tag systemadapter's FIRST tag at the then-current family version (v4.9.0 as of the 2026-09-01 train) via `scripts/verify-tag.sh`, and strict drift + release-train exemptions drop to a true zero. Source: 2026-08-30/31 sweep sessions.
- [ ] **bench-spike gate re-evaluation post-v4.9.0 (deferred under load).** The v4.9.0 train could not honestly run the bench gate: concurrent session load 5.9+ (playwright/chromium from unrelated work) produced a uniform +14-31% "regression" across ALL sub-benches including the appkit-free `json-roundtrip` — machine noise, not code. setup now resolves go-appkit v0.3.0 from the proxy (replace stripped at v4.9.0), so the appkit-service sub-bench legitimately measures a different dependency resolution: on an IDLE machine, re-run `nix run .#bench-spike`; if appkit-service alone exceeds the 10% gate with the other two green, re-pin via `--save-baseline` in the same change and commit `docs/benchmarks/setup-baseline.raw.txt`. Never re-pin under load.

---

## P2 — Medium impact (tooling & quality)

- [ ] **DataStar Tier 4 — demand-gated panel variants (ADR-0050 ACCEPTED 2026-09-07; Tiers 1-3 SHIPPED, see CHANGELOG).** Strategy ADR: `docs/adr/0050-dual-frontend-protocol-strategy.md` (route-split accepted, Model B rejected, setup opt-in dependency decided, `/ds/events` session-gated 401, sequenced vs the open `/sse` decision by adoption). Shipped: `setup.Config.DataStarPath`/`DataStarScriptPath` + `Bundle.DataStarBroadcaster` (shared hub with `/sse`), route-split recipe in `fullstack-wiring.md` with a passing contract test (`integration_test/datastar_route_split_test.go`), setup-demo dual-transport showcase (`/ds-demo`, `POST /broadcast` fans out to both transports). REMAINING (each gated on consumer demand evidence per ADR-0050 §Decision 1 — plan Tier 4: `docs/planning/2026-09-07_17-18_datastar-dual-frontend-rollout.html`): (M11) dashboardui signal-patch spike + go/no-go; (M12) dashboardui DataStar variant module; (M13) loginpage signal forms; (M14) offline-sync evaluation vs sync-worker.js; (M15) adminui variant design doc; (M16) release train for the changed modules (setup carries the new API; its publish is part of the next family train regardless of Tier 4).

- [~] **Wire remaining `check-*` apps into CI.** `check-docs-links`, `check-service-methods`, `check-domain-counts`, `check-large-files`, `check-require-tags` (renamed from `check-phantom-version` 2026-08-30; the CI step is the inline zero-pseudo grep), `check-codegen` (templ pinned @v0.3.1020), and `check-templates` (hermetic GOWORK=off rework + CI step with clean-restore assertion, 2026-08-15) now run in CI. Remaining: `check-cqrs-lint` (blocked: Nix-only binary; needs Go-installable distribution — see P3.1). New modules health + auditlog are wired into CI build/test/coverage/lint/mod-tidy (2026-08-14). DECIDED (2026-08-29): `bench-spike` stays LOCAL-only — the raw baseline is machine-pinned and CI runners are a different machine population (env guard exits 2 by design); see docs/benchmarks/README.md (annotation comment added to ci.yml 2026-08-30). `check-release-train` runs ADVISORY in CI since 2026-08-30 and is now also a LOCAL pre-commit gate (fail on UNPUBLISHED; offline exit 4 only warns). Repo visibility is CONFIRMED PUBLIC (2026-08-30, anonymous ls-remote works for the whole family), so Actions CAN list tags — the advisory output is meaningful. OPEN: flip CI to blocking after one full green advisory week (week began 2026-08-30); the advisory also gains `--strict-lag 0` at the same time. `check-go-toolchain` is BLOCKING in CI (2026-08-30, hermetic).

- [ ] **Decisions awaiting the user (2026-08-16 status report §g):** (1) ~~family tag v4.8.1/v4.9.0 timing~~ **RESOLVED 2026-08-30:** the user approved runbook execution; `usermgmt/v4.8.1`, `usermgmt/totp/v4.8.1` (first tag since v4.7.0), `usermgmt/oauth2/v4.8.1` cut+pushed, plus `setup/v4.8.3` + `dashboardui/v4.8.2` (the locally-cut dashboardui/v4.8.1 proved stale — see runbook status); all 5 DEV-ONLY replaces stripped; admin-demo totp pin bumped to v4.8.1. (2) `/sse` authz posture — session-gating vs adding a stream-type filter before the endpoint shape is published. SPIKED 2026-08-30 (`transport/filtered_sse_spike_test.go`) and MECHANISM SHIPPED 2026-08-30 (`transport.WithSSEFilter` + fail-closed `filteredEventStore` replay wrapper; documented in `docs/guides/sse-and-datastar.md` §Scoped Feeds). Decision one-pager with options table + recommendation (A ship as-is, B scoped option, C per-user streams, D drop shared /sse): `docs/planning/2026-08-30_sse-endpoint-shape-decision.md`. The decision is now purely a product/security call, not a feasibility question. RESOLVED: (3) Go toolchain — v4.8.1 shipped on 1.26.7 (2026-08-29); NOTE: CORS posture decided (2026-08-21) — same-origin default, consumer wraps with `httputil.CORS`; documented in `docs/guides/sse-and-datastar.md`.
- [ ] **Tooling debt bundle (harvested 2026-09-09 docs-health, from docs/status/2026-09-09 §f6/f7/f10/f11/f12):** (a) extend `docs-freshness` gate to catch replace-state claims and "uniform at vX" assertions (both were provably wrong pre-session while the gate stayed green); (b) one-command dependency-bump gate bundle (build, test, vet, lint, check-modules, check-cqrs-lint, bench sanity — closes the 09-09 bump's ladder hole); (c) self-test for `check-version-drift.sh`'s replace exemption (fixture with/without replace; repo precedent: `test-install-git-hooks.sh`); (d) refactor drift script so tag-check and drift-check share ONE replace-exemption implementation (kills the split brain at root); (e) upstream ask: teach go-cqrs-lite `cqrs-upgrade` multi-module/workspace mode (it parses ONE consumer go.mod; this repo needed 22 manual runs).
- [ ] **setup CSRF knob + rejection test (recurring since 2026-08-14).** `Mount` hardcodes `httputil.CSRFConfig{}` (setup/bundle.go:112 — Secure=false startup WARN); no Config knob, and no test asserts a tokenless mutation POST is rejected. Add `Config.CSRF` (or a documented security note) + a CSRF-rejection regression test. Sources: docs/status/archived/2026-08-14_13-20 §c7/c8, 2026-08-14_13-58 item 20.
- [ ] **Docs debt bundle (harvested 2026-09-09 docs-health):** (a) ~~`.agents/skills/cqrs-htmx/SKILL.md` + `references/realtime.md` teach deprecated `Raw()`/`NewBroadcasterFromRaw`~~ **DONE 2026-09-09** (updated to `Hub()`/`NewBroadcasterFromHub`); (b) write `docs/guides/actor-and-audit-trail.md` (requested 2026-08-13 ×2, never written); (c) examples/basic actor-metadata demo; (d) `examples/async-startup-demo/` (deferred item from the async-startup plan, never created); (e) mount `health.NewProbe` + `auditlog.WithAuditLog` in an example AND the auditlog viewer in the fullstack UI integration test (the bridge modules ship with zero end-to-end wiring proof); (f) setup/README.md: Troubleshooting section + security/TLS-termination note. (Withdrawn during verification: the alleged dangling `docs/review/2026-08-16...` reference in setup Go comments does not exist.) Smaller residuals (script hardening, docs micro-fixes, example smoke tests) live in ROADMAP → "Residual micro-ideas (harvested 2026-09-09 docs-health sweep)".

---

## P3 — Technical debt & future

- [ ] **Migrate usermgmt off go-cqrs-lite v5-removed APIs (39 V007 findings, surfaced 2026-09-09 by `cqrs-upgrade -dry-run`).** Three clusters, all in usermgmt: (1) `stack.Materialize` ×3 (`es_materialize_adapter.go:48,65,79`) → metaengine auto-projection (ADR-0123); (2) `stack.Bundle` ×2 (`es_setup_core.go:35`, `stack_repositories.go:26`) → `system.New` composition (ADR-0123 — the systemadapter module already demonstrates this path); (3) `storage.SQLViewStore` family ×34 (`sql_readmodel.go`, `sql_readmodel_extra.go`, `sql_readmodel_mysql.go`) → metaengine engines with layout planning (ADR-0126). All 39 still compile and test green on the 2026-09-08 v4 trains (verified 2026-09-09 post-bump) — this is v5-prep, not a v4 blocker. Re-surface with `cqrs-upgrade -dry-run` (go-cqrs-lite `cmd/cqrs-upgrade`); the V007 list is drift-checked against upstream's `Deprecated:` markers. Sequencing: cluster (2) overlaps the P1 systemadapter-tag work; cluster (3) is the largest and should follow metaengine layout-planning API maturity.
- [ ] **Upstream ask (go-cqrs-lite): decouple `stack/v4`'s root package from `metaengine/v4`.** Re-scoped 2026-08-29 after tracing the former "drop the stale metaengine/v4 require from usermgmt/go.mod" item — removal from usermgmt's side is NOT possible: usermgmt imports `stack/v4` (es_materialize_adapter.go, es_setup_core.go, stack_repositories.go, sqlite_setup.go), and stack v4.3.0's own root-package files (`options.go`, `bundle.go`) import metaengine packages, so metaengine is a genuine transitive package dependency and `go mod tidy` correctly keeps the `// indirect` require. The 2026-08-16 premise ("no Go file under usermgmt imports metaengine") was true but irrelevant — package imports by dependencies are the edge. Only an upstream split of stack's option/bundle layer from metaengine removes it. Source: composability round-2 review, finding 6 + this trace.
- [~] **Adopt appkit as the `setup` server layer (ADR-001 Option A confirmed by spike).** PROGRESS 2026-09-07: `RunWithAppkit` is promoted from spike to stable setup API (spike markers removed; go-appkit pinned to published v0.4.0, no replace; SSE-flush/readiness/parity tests race-green). REMAINING (the ADR-001 default-flip decision, gated on the DataStar ADR-first sequencing): (b) fold into `RunHandler` behind the unchanged signature; (c) `/health` dedup + LB guidance; (d) stacked-chain dedup (bundle recovery + security headers); (e) logging posture; (f) expose `Addr()`. The `spike/appkit-server` branch carries `RunWithAppkit` + passing SSE-flush/readiness/parity tests and the adopt report (`docs/status/archived/2026-08-16_01-38_appkit-spike-adopt.md`): hardened benchmark (5× runs, `-benchmem`, `LogLevelError` to isolate stack from logging — see CHANGELOG) shows baseline-httputil ~17µs/op @ ~60k req/s vs appkit-service ~20µs/op @ ~50k req/s, ~30 extra allocs/op per request being the real cost; drain-phase and projection-aware readiness both verified under `-race`. **READY (2026-08-16):** go-appkit core v0.3.0 (`NoTimeout` + `ReadyCheck`) is cut at `f938d65` together with cqrs/v0.3.0, realtime/v0.1.0, flightrecorder/v0.1.0 — push is the only remaining step (user gate; see go-appkit TODO_LIST P1). Fold-in must resolve in ONE change: (a) drop the spike `replace`, point `go-appkit` at v0.3.0; (b) fold `RunWithAppkit` into `RunHandler` behind the unchanged signature; (c) `/health` semantics — suppress the bundle's duplicate `/health` when appkit owns health, document that LBs must target `/health/ready` (comparison finding 2: the meaning silently flips readiness→liveness in appkit mode); (d) dedupe the stacked chains — drop bundle recovery (appkit's is outermost) and the duplicate security-header pass (finding 3); (e) decide the logging posture — wire setup's logger or configure the appkit level; the 2.8x bench delta in the spike was entirely per-request INFO logging (finding 7 — now isolated by `LogLevelError` in the hardened bench); (f) consider exposing `Addr()` (new capability). Sources: comparison report `docs/review/2026-08-16_setup-vs-go-appkit-comparison.md` §4/§6/§8 (findings 1-3, 7).
- [ ] **Add cqrs-lint strict CI gate to GitHub Actions.** Run cqrs-lint `--strict` in CI to catch catalog/validation findings early. Blocked: cqrs-lint is a Nix-only binary; needs a Go-installable distribution or a Nix CI runner. The flake.nix `check-cqrs-lint` app exists for local use.
- [ ] **Cross-module dep version drift after v4.7.0 tagging.** Published submodule tags reference stale sibling versions (e.g., `adminui/v4.7.0` still depends on `usermgmt/v4.6.1`). Next release should bump all cross-module refs before tagging. Source: `docs/status/archived/2026-08-07_22-23_release-v4.7.0-and-retry-investigation.md`. CURRENT SPLITS (2026-09-09, post go-cqrs-lite 2026-09-08-train bump): RESOLVED for all larsartmann deps — the alignment sweep brought integration_test (the last straggler: templ-components v1.11.0→v1.14.0, go-datastar v0.3.0→v0.5.0, go-health v0.0.2→v0.1.3, go-health-dashboard v0.3.0→v0.6.1, go-idempotency v0.2.0→v0.3.0, otel v4.3.0→v4.4.0) and all metaengine pins to v4.13.0; `check-version-drift --strict` is GREEN (660 requires, all published). NOTE (2026-09-09): "drift ✅" means INTERNAL uniformity — upstream is ahead by ~122 advisory train-lag entries (templ-components v1.16.0 leading; full list in the P1 next-train sweep item); do not read "release-train ✅" as "current with the world". The drift scan now skips requires satisfied by a local replace in the same go.mod (same exemption the tag-existence check always applied), so the 2 modules carrying the projectionadapter replace (systemadapter, examples/system-demo) can no longer false-positive when the replaced sibling moves. Stage status on 2026-09-09 HEAD: isolation ✅, dep-budgets ✅ (setup justified 18→19), go-toolchain ✅, drift --strict ✅, release-train ✅, replace-directives ✅.
- [ ] **Rewrite `origin/v4` branch history to strip 3 remaining binary blobs (~27.7 MB).** The master branch was cleaned via `git filter-repo` (731.8 MB → 0 blobs), but the `v4` branch has independent binary contamination (`examples/basic/basic` 9.8MB, `examples/datastar-demo/datastar-demo` 8.9MB ×2) that does not share ancestry with master. Requires `git filter-repo` on the v4 branch + force-push. Source: `docs/status/2026-08-09_06-15_git-binary-cleanup-history-rewrite.md`.
- [ ] **Purge `examples/setup-demo/setup-demo` (27 MB) from pushed master history.** Removed from HEAD and from all unpushed commits 2026-08-14 (filter-branch; family tags re-pointed), but the blob still exists in already-pushed history (introduced `5604e810`, last carried by `73ff1556`). Requires filter-repo + force-push on master — same class as the v4 branch item above. Low priority: the repo already survived a 731.8 MB cleanup.
- [ ] **Hardware: `/mnt/buildcache` (sda1) is FULLY DEAD since 2026-08-22** — began failing 2026-08-16 with I/O errors, now the mount is gone entirely (`df` reports "No such device"); `GOCACHE`/`GOMODCACHE`/`GOLANGCI_LINT_CACHE` pointed there, so every gate fails uniformly with `failed to initialize build cache`. The documented `$HOME/.cache` workaround ALSO no longer works (`~/.cache/go-build` is a read-only nix-store symlink). Current working workaround: `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOLANGCI_LINT_CACHE=/tmp/golangci-cache` (mkdir first; prefix `git commit` too so the pre-commit hook inherits them; cold /tmp module cache re-downloads deps). Root FS (nvme) healthy. Needs hardware attention; NOT fixable from this repo.
- [ ] **SSE hardening backlog (from docs/status/archived/2026-08-21_21-39 §f — untracked until 2026-09-09):** SSEMaxReplay input validation/clamp (setup config validate() has no check); `transport/serve_test.go` gaps (replay-before-subscribe ordering, heartbeat join-on-exit, broadcaster-close-mid-stream, concurrent clients); `WithSSEMaxReplay` handler option; `SSEOptions` shared struct; `Retry` field on replayed domain events; `JournalSSEStore.EventsAfter` bench on large journals; fuzz `DomainEventToSSE`; e2e `/sse` reconnect-with-replay scenario; cross-module identical-wire-format contract test. Genuinely optional — route individually on demand.
- [ ] **ProjectionLayer retirement prep (Phase 5 of the declarative-projections plan).** Add `// Deprecated:` to `NewProjectionLayer` (systemadapter/projections.go:98), write the declarative-vs-ProjectionLayer migration guide, migrate `examples/system-demo` off `NewProjectionLayer` (main.go:52), add SQLite-engine declarative tests + negative-path tests (no `TestDeclarative_SQLite*` exists). v5-removal item alongside ADR-0123. Sources: docs/status/archived/2026-08-09_13-51, docs/plans/archived/2026-08-09_metaengine-fold-declarations.md §Phase 5.
- [ ] **Micro-debt bundle (harvested 2026-09-09 docs-health):** (a) `setup.NewFromSystem()` bridge — requested 2026-08-09 ×2, never decided: build it or explicitly reject in ROADMAP; (b) `usermgmt/http.go:315` ignored `w.Write` return (same anti-pattern the 2026-08-09 typed-error review fixed elsewhere); (c) `usermgmt/service_oauth2_errorcontext_test.go:55` still uses legacy `errors.As(err, &ef)` pattern; (d) extract `slowJournal` test helper to a shared testutil (recurring ask since 2026-08-14; currently local to integration_test/async_startup_test.go:26); (e) loginpage polish: emoji-brand favicon fallback renders broken glyphs, error styling hardcoded light-mode, "no auth configured" copy; (f) decide `examples/datastar-demo` rebrand-or-remove (modularization assessment 2026-07-19 item 5).

---

_For completed work, see [CHANGELOG.md](CHANGELOG.md) and [git log](https://github.com/larsartmann/cqrs-htmx/commits/master). For long-term vision, v5 plans, and rejected ideas, see [ROADMAP.md](ROADMAP.md)._
