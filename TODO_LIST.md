# TODO List — cqrs-htmx

> Short-term, actionable, bounded work. Open items only.
> Completed work lives in [CHANGELOG.md](CHANGELOG.md). Long-term vision, v5 plans, and rejected ideas live in [ROADMAP.md](ROADMAP.md).

**Updated:** 2026-09-17 | **Version:** v4.9.0 family train (13 tags, 2026-09-01) + `setup/v4.10.0` (2026-09-07) + go-cqrs-lite 2026-09-08-train bump (2026-09-09, advisory lag zeroed; CI gates BLOCKING with `--strict-lag 0`) + OTel sprint 64→90 (2026-09-14) | **Modules:** 27 in `go.work` (15 production + 11 examples + e2e/server) | **`*Service` methods:** 73 (leading v5 indicator; see ROADMAP) | **Coverage:** all 15 gates green (spot-verified 2026-09-10) | **Lint:** 0 issues / 15 modules | **appkit wave SHIPPED:** go-appkit v0.4.0 published, `RunWithAppkit` stable since 2026-09-07 (ADR-001 default-flip remains, see P3) | **2026-09-15 truth sweep:** the list below was re-verified against the tree; ~12 items completed in prior sessions were removed (CI blocking flip, systemadapter unlink bug, next-train alignment sweep, setup CSRF knob + rejection test, docs/tooling/micro-debt bundles incl. actor-audit guide + async-startup-demo + health/auditlog wiring proofs, SSEMaxReplay validation) | **2026-09-17:** setup deep-dive audit (report `docs/research/2026-09-17_setup-deep-dive.html`; actionable remainder routed to the P1 setup gap bundle).

## Status Legend

- [ ] **OPEN** — actionable, not yet started.
- [~] **PARTIALLY DONE** — started but incomplete.

> No `[x]` items here. When a task finishes, it moves to [CHANGELOG.md](CHANGELOG.md) and is removed from this list. Deferred/rejected ideas move to [ROADMAP.md](ROADMAP.md) → "Not Planned".

---

## P1 — High impact (release follow-through & doc health)

- [ ] **bench-spike idle re-run (sole remainder of the post-bump deferred verification pass).** Everything else in the 2026-09-10 pass is green (coverage-gate 15/15, test-race, test-fuzz, test-flake, check-cqrs-lint, e2e 4/4). The v4.9.0 train swapped go-appkit resolution (replace → published v0.4.0), so the appkit-service sub-bench legitimately measures a different dependency resolution: on an IDLE machine, re-run `nix run .#bench-spike`; if appkit-service alone exceeds the 10% gate with the other two green, re-pin via `--save-baseline` in the same change and commit `docs/benchmarks/setup-baseline.raw.txt`. Never re-pin under load — refused again 2026-09-15 (load 825/32 cores from a concurrent session). The gate's own `BENCH_MAX_LOAD` guard enforces this mechanically.
- [ ] **systemadapter first tag + final replace strip (blocked on ONE upstream tag).** go-cqrs-lite published `metaengine/v4 v4.12.0`, `metaengine/sqliteengine/v4 v4.2.0`, `event v4.9.0`, `metadata v4.6.0`, `record v4.4.0`, `commandlifecycle v4.0.1` — the drift axes closed repo-side and systemadapter dropped its sqliteengine + metaengine replaces (build/vet/test green hermetically). REMAINING: `metaengine/projectionadapter/v4 v4.5.0+` (v4.4.1 is the published max; `OccurredAt` on EventWithID needs v4.5.0) → then strip the last replace (systemadapter + examples/system-demo), tag systemadapter's FIRST tag at the then-current family version via `scripts/verify-tag.sh`, and strict drift + release-train exemptions drop to a true zero. Version-number question tracked as ROADMAP Open Question 12. Source: 2026-08-30/31 sweep sessions.
- [ ] **setup composition-root gap bundle (from the 2026-09-17 setup deep-dive, `docs/research/2026-09-17_setup-deep-dive.html`; adoption 72/100, 0 misused APIs — every item is an additive opt-in seam).** Three "Do" items in Pareto order: (1) `setup.Config.AuthHandlerConfig *usermgmt.HandlerConfig` escape hatch — usermgmt's six per-group auth rate limiters (`WebAuthnRateLimit` et al., usermgmt/http.go:74-88), cookie `Secure`/`SessionMaxAge`, handler `Timeout`, and `OAuth2SuccessURL`/`OAuth2ErrorURL` are unreachable today (setup.go:57 threads only `CookieName`; no setters on AuthHandler; the `ServiceConfig`-verbatim policy covers the service layer, not this HTTP-layer struct) — conflict-validate CookieName like the other seams; (2) fail-fast `ReadModelDB.Driver()` validation at `New`: the flattened path never sets `ReadModelDialect`, and `""` resolves to SQLite constructors (usermgmt/sql_readmodel_dialect.go:31), so a Postgres/MySQL `*sql.DB` via the documented flattened field builds SQLite-schema read models — reject non-sqlite drivers with a pointer to `ServiceConfig.ReadModelDialect` (no new knob; policy intact); (3) go-appkit v0.4.0 → v0.5.0 (published 2026-09-17) + `Config.Metrics`/`Config.Version` threaded into run_appkit.go's ServiceConfig — v0.5.0 ships a dependency-free Basic-Auth-gated Prometheus `/metrics` and `/version` build-info that `RunWithAppkit` cannot currently enable, and neither serve path offers any metrics/build-info endpoint. "Consider" tier from the same report: `Config.RequestLogging` opt-in (root logging.go:204 unused by setup; RunHandler serves with zero access logs), opt-in session-gated mounts for `EventCatalogHandler(usermgmt.DefaultEventCatalog())`/`ProjectionStatusHandler`/`DebugHandler`, liveness path + `HealthPath` "-" opt-out (empty is silently re-defaulted, config.go:294), and sse.js extension mount next to SSEPath (DataStarScriptPath symmetry). README security-paragraph staleness was fixed in the audit session (it still claimed "no CSP/HSTS/CSRF middleware is on by default").

---

## P2 — Medium impact (tooling & quality)

- [ ] **OTel posture is at the sprint target (64→90) — follow-ups go to ROADMAP, not here.** The 2026-09-13 audit's five opportunities (free domain spans docs, HTTP root-span recipe + observability-demo wiring, correlation recipe, `ServiceConfig`/setup observability seams, SSE fan-out recipe) all landed 2026-09-14 — see CHANGELOG. Any NEW observability work (logs SDK, profiles, in-library SSE spans — all deliberately out of scope) starts as a ROADMAP idea, not a TODO here.

- [ ] **DataStar Tier 4 — demand-gated panel variants (ADR-0050 ACCEPTED 2026-09-07; Tiers 1-3 SHIPPED, see CHANGELOG).** Strategy ADR: `docs/adr/0050-dual-frontend-protocol-strategy.md` (route-split accepted, Model B rejected, setup opt-in dependency decided, `/ds/events` session-gated 401, sequenced vs the open `/sse` decision by adoption). Shipped: `setup.Config.DataStarPath`/`DataStarScriptPath` + `Bundle.DataStarBroadcaster` (shared hub with `/sse`), route-split recipe in `fullstack-wiring.md` with a passing contract test (`integration_test/datastar_route_split_test.go`), setup-demo dual-transport showcase (`/ds-demo`, `POST /broadcast` fans out to both transports). REMAINING (each gated on consumer demand evidence per ADR-0050 §Decision 1 — plan Tier 4: `docs/planning/2026-09-07_17-18_datastar-dual-frontend-rollout.html`): (M11) dashboardui signal-patch spike + go/no-go; (M12) dashboardui DataStar variant module; (M13) loginpage signal forms; (M14) offline-sync evaluation vs sync-worker.js; (M15) adminui variant design doc; (M16) release train for the changed modules (setup carries the new API; its publish is part of the next family train regardless of Tier 4).

- [~] **Wire remaining `check-*` apps into CI.** All gates except `check-cqrs-lint` run in CI (docs-links, service-methods, domain-counts, large-files, require-tags, codegen, templates, go-toolchain, release-train BLOCKING with `--strict-lag 0` since 2026-09-09 — the flip's first run caught a real templ-components drift). Remaining: `check-cqrs-lint` (blocked: Nix-only binary; needs Go-installable distribution — see P3). `bench-spike` stays LOCAL-only by decision (machine-pinned baseline; see docs/benchmarks/README.md).

- [ ] **Decision awaiting the user: `/sse` authz posture** — session-gating vs adding a stream-type filter before the endpoint shape is published. SPIKED + MECHANISM SHIPPED 2026-08-30 (`transport.WithSSEFilter` + fail-closed `filteredEventStore`; documented in `docs/guides/sse-and-datastar.md` §Scoped Feeds). Decision one-pager with options table + recommendation (A ship as-is, B scoped option, C per-user streams, D drop shared /sse): `docs/planning/2026-08-30_sse-endpoint-shape-decision.md`. Purely a product/security call now, not a feasibility question. (Former items (1) tag timing and (3) Go toolchain: RESOLVED — see CHANGELOG.)

- [ ] **Adminui prettier + gap-remediation program (awaiting execution approval).** Pareto plan: `docs/planning/2026-09-17_13-43_adminui-prettier-gap-remediation-plan.md` (27 medium tasks / 101 micro tasks; 1%→51% = Flush+icons+badge-dots+StatCard sweep; 4%→64% = PageHeader/Breadcrumbs/FilterInput; 20%→80% = Pagination+loading states+errorpage; rest = Modal/Dropdown/PolledRegion/dark-QA). Source audit: `docs/research/2026-09-17_templ-components-deep-dive.html` (score 68/100; F1–F11). Preconditions inside the plan: rebuild stale buildflow binary + triage the hook's examples/* lint failures (P0.2), confirm render-test semantics (P0.1).
- [ ] **Dashboardui templ-components adoption program (28 medium tasks, in execution).** Pareto plan: `docs/planning/2026-09-17_13-22_dashboardui-templ-components-pareto-execution-plan.md` (Tier 0 session debt → Tier 1 Tailwind v4 gate + StatusBadge spike → Tier 2 errorpage/badges/stat cards → Tier 3 EmptyState/Buttons/Toast+nonce/GlobalErrorHandling/CopyButton/3 sortable tables → Tier 4 pagination/theme/sidebar/CSS-cleanup/security+golden+bench+a11y hardening/re-score; gate-first — nothing adoptable before the Tailwind pipeline exists). Source audit: `docs/research/2026-09-17_templ-components-dashboardui-deep-dive.html` (score 14/100, 1 of 22 applicable capabilities; companion adminui audit 68/100, different subject, adjudicated 2026-09-17). Hybrid path verified: leaf components render into the existing `strings.Builder` via `Component.Render(ctx, &b)` — no templ conversion required.

---

## P3 — Technical debt & future

- [ ] **Migrate usermgmt off go-cqrs-lite v5-removed APIs (39 V007 findings, surfaced 2026-09-09 by `cqrs-upgrade -dry-run`).** Three clusters, all in usermgmt: (1) `stack.Materialize` ×3 (`es_materialize_adapter.go:48,65,79`) → metaengine auto-projection (ADR-0123); (2) `stack.Bundle` ×2 (`es_setup_core.go:35`, `stack_repositories.go:26`) → `system.New` composition (ADR-0123 — the systemadapter module already demonstrates this path); (3) `storage.SQLViewStore` family ×34 (`sql_readmodel.go`, `sql_readmodel_extra.go`, `sql_readmodel_mysql.go`) → metaengine engines with layout planning (ADR-0126). All 39 still compile and test green on the 2026-09-08 v4 trains (verified 2026-09-09 post-bump) — this is v5-prep, not a v4 blocker. Re-surface with `cqrs-upgrade -dry-run` (go-cqrs-lite `cmd/cqrs-upgrade`); the V007 list is drift-checked against upstream's `Deprecated:` markers. Sequencing: cluster (2) overlaps the P1 systemadapter-tag work; cluster (3) is the largest and should follow metaengine layout-planning API maturity.
- [ ] **Upstream asks (go-cqrs-lite).** (1) Decouple `stack/v4`'s root package from `metaengine/v4` — re-scoped 2026-08-29: removal from usermgmt's side is NOT possible; stack's own root-package files (`options.go`, `bundle.go`) import metaengine packages, so only an upstream split of stack's option/bundle layer removes the transitive require. Source: composability round-2 review, finding 6 + trace. (2) Teach `cqrs-upgrade` multi-module/workspace mode — it parses ONE consumer go.mod; the 2026-09-09 sweep needed 22 manual runs across this repo's 27 modules.
- [~] **Adopt appkit as the `setup` server layer (ADR-001 Option A confirmed by spike).** PROGRESS 2026-09-07: `RunWithAppkit` is promoted from spike to stable setup API (spike markers removed; go-appkit pinned to published v0.4.0, no replace; SSE-flush/readiness/parity tests race-green). REMAINING (the ADR-001 default-flip decision, gated on the DataStar ADR-first sequencing): (b) fold into `RunHandler` behind the unchanged signature; (c) `/health` dedup + LB guidance (appkit mode silently flips readiness→liveness — comparison finding 2); (d) stacked-chain dedup (drop bundle recovery + duplicate security-header pass); (e) logging posture (the 2.8x bench delta was entirely per-request INFO logging, now isolated by `LogLevelError` in the hardened bench); (f) consider exposing `Addr()`. Sources: comparison report `docs/review/2026-08-16_setup-vs-go-appkit-comparison.md` §4/§6/§8, adopt report `docs/status/archived/2026-08-16_01-38_appkit-spike-adopt.md`.
- [ ] **Add cqrs-lint strict CI gate to GitHub Actions.** Run cqrs-lint `--strict` in CI to catch catalog/validation findings early. Blocked: cqrs-lint is a Nix-only binary; needs a Go-installable distribution or a Nix CI runner. The flake.nix `check-cqrs-lint` app exists for local use.
- [ ] **Rewrite `origin/v4` branch history to strip 3 remaining binary blobs (~27.7 MB).** The master branch was cleaned via `git filter-repo` (731.8 MB → 0 blobs), but the `v4` branch has independent binary contamination (`examples/basic/basic` 9.8MB, `examples/datastar-demo/datastar-demo` 8.9MB ×2) that does not share ancestry with master. Requires `git filter-repo` on the v4 branch + force-push. Source: `docs/status/2026-08-09_06-15_git-binary-cleanup-history-rewrite.md`.
- [ ] **Purge `examples/setup-demo/setup-demo` (27 MB) from pushed master history.** Removed from HEAD and from all unpushed commits 2026-08-14 (filter-branch; family tags re-pointed), but the blob still exists in already-pushed history (introduced `5604e810`, last carried by `73ff1556`). Requires filter-repo + force-push on master — same class as the v4 branch item above. Low priority: the repo already survived a 731.8 MB cleanup.
- [ ] **Hardware watch: `/mnt/buildcache` has a failure history.** The mount died 2026-08-16→2026-08-22 (every gate failing uniformly with "failed to initialize build cache") and was later repaired — as of 2026-09-15 it is healthy again (sdb1, 220G, build-verified). If it dies again: repoint `GOCACHE`/`GOMODCACHE`/`GOLANGCI_LINT_CACHE` to `/tmp` (mkdir first; prefix `git commit` too so the pre-commit hook inherits them; `scripts/lib/go-cache-env.sh` has the low-disk guard and fallback), and check `df -h /mnt/buildcache` first when gates fail en masse. Needs hardware attention if it recurs; NOT fixable from this repo.
- [ ] **SSE hardening backlog (from docs/status/archived/2026-08-21_21-39 §f).** ~~SSEMaxReplay input validation~~ DONE 2026-09-10 (setup `validate()` rejects negative values). Remaining (genuinely optional — route individually on demand): `transport/serve_test.go` gaps (replay-before-subscribe ordering, heartbeat join-on-exit, broadcaster-close-mid-stream, concurrent clients); `WithSSEMaxReplay` handler option; `SSEOptions` shared struct; `Retry` field on replayed domain events; `JournalSSEStore.EventsAfter` bench on large journals; fuzz `DomainEventToSSE`; e2e `/sse` reconnect-with-replay scenario; cross-module identical-wire-format contract test.
- [ ] **Remove `ProjectionLayer` in the v5 removal bundle (all prep DONE 2026-09-15).** `// Deprecated:` marker shipped (systemadapter/projections.go:99); migration guide `docs/guides/declarative-projections.md` (API mapping, drain semantics, checkpoint-store caveat); `examples/system-demo` migrated off `NewProjectionLayer` (now uses `sys.Start` + declarative queries); SQLite-engine declarative tests (`TestDeclarative_SQLite_UserLifecycle`, `TestDeclarative_SQLite_AuthzPolicies`) + negative-path tests (`TestDeclarative_MissingLookups`) landed; adminui/dashboardui verified ProjectionLayer-free; test migration (plan step 5.3) is covered by the declarative suite + `TestDeclarative_EquivalenceWithProjectionLayer` (the remaining ProjectionLayer tests legitimately test the deprecated layer itself). One honest gap to resolve before removal: the declarative path has no custom checkpoint/DLQ store injection yet (system.New uses an internal in-memory checkpoint store) — needs an upstream `system.New` option or an accepted-limitation note in the v5 removal inventory.
- [ ] **Decide `examples/datastar-demo` rebrand-or-remove** (modularization assessment 2026-07-19 item 5). Owner call. (`setup.NewFromSystem()` bridge: routed to ROADMAP Open Question 12 — build-or-reject decision lives there.)

---

_For completed work, see [CHANGELOG.md](CHANGELOG.md) and [git log](https://github.com/larsartmann/cqrs-htmx/commits/master). For long-term vision, v5 plans, and rejected ideas, see [ROADMAP.md](ROADMAP.md)._
