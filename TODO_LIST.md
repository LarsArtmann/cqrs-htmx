# TODO List — cqrs-htmx

> Short-term, actionable, bounded work. Open items only.
> Completed work lives in [CHANGELOG.md](CHANGELOG.md). Long-term vision, v5 plans, and rejected ideas live in [ROADMAP.md](ROADMAP.md).

**Updated:** 2026-09-22 (round 7) | **Round 7 (docs-health completion):** both status gates repo-owned + CI-wired with fixture self-tests (Gate 2 row-integrity checker + 17-case test; Gate 1 self-test, first CI run ever), GOTOOLCHAIN floor alignment fixes the pre-commit hook from bare shells (proven live), ADR-0051 Accepted, living-doc headers swept to v4.12.0 | **Round 6 (design-tree interview + execution):** user decisions locked (theme Option B, /sse Option B scoped option, v4.12.0 after the batch, force-push history rewrites WAIVED → ROADMAP Not-Planned, stack/v4 to be REMOVED in go-cqrs-lite v5); templ-components **v1.19.0 cut from this workspace** (popover capture+self-heal fix + `DropdownProps.Trigger`) and adopted repo-wide; v4.11.0 train follow-through | **Version:** v4.12.0 family train (14 tags, 2026-09-22, pushed; CI green) | **Modules:** 28 in `go.work` | **Coverage:** all 15 gates green (`nix run .#coverage-gate` 2026-09-20) | **Lint:** 0 issues / 15 modules

## Status Legend

- [ ] **OPEN** — actionable, not yet started.
- [~] **PARTIALLY DONE** — started but incomplete.

> No `[x]` items here. When a task finishes, it moves to [CHANGELOG.md](CHANGELOG.md) and is removed from this list. Deferred/rejected ideas move to [ROADMAP.md](ROADMAP.md) → "Not Planned".

---

## P1 — High impact (release follow-through & doc health)

- [ ] **bench-spike idle re-run (sole remainder of the post-bump deferred verification pass).** Everything else in the 2026-09-10 pass is green (coverage-gate 15/15, test-race, test-fuzz, test-flake, check-cqrs-lint, e2e 4/4 + admin-behavior 9/9). The v4.9.0 train swapped go-appkit resolution (replace → published v0.4.0), so the appkit-service sub-bench legitimately measures a different dependency resolution: on an IDLE machine, re-run `nix run .#bench-spike`; if appkit-service alone exceeds the 10% gate with the other two green, re-pin via `--save-baseline` in the same change and commit `docs/benchmarks/setup-baseline.raw.txt`. Never re-pin under load — refused 2026-09-15 (load 825/32), 2026-09-17 (load 16.4 vs limit 8), 2026-09-20 ×2 (load 72 and 19.9 vs limit 8), and twice more during the 2026-09-21/22 train (8 documented refusals; external workloads on 32 cores). The gate's own `BENCH_MAX_LOAD` guard enforces this mechanically. **Structural question (owner): automate or retire** — see ROADMAP Open Questions.

---

## P2 — Medium impact (tooling & quality)

- [ ] **templ-components upstream asks — RECORDED 2026-09-22 (D1) in the owning repo's tracker** (we own templ-components; its TODO_LIST "Harvested 2026-09-22 — cqrs-htmx adoption asks" carries items 230–232). Local record kept for traceability:** (a) `display.ListNote`: add a range/list semantics variant — consumers showing "N items in range" need count-only or per-item, never both; (b) `display.Grid`: document that `{children...}` is empty when the component is rendered standalone into a strings.Builder (hybrid path) — or support data-props children; (c) `display.CopyButton`: the inner `span[data-tc-copy-text]` inherits ancestor color in consumers whose unlayered table CSS overrides it — consider an explicit default color on the span. Upstream repo: github.com/larsartmann/templ-components (local checkout `~/projects/templ-components`; we own it — asks (d) `DropdownProps.Trigger` and (e) popover-positioner capture+self-heal were cut directly as v1.19.0, 2026-09-21).
- [ ] **SidebarNav revisit criteria (dashboardui).** Custom dark sidebar stays while ALL of: (1) it renders in the string-builder hybrid path without children-slot loss, (2) mobile drawer keeps zero-JS, (3) the library ships a dark-token shell matching `--sidebar-bg`/`--tc-sidebar-*` variables. Re-evaluate at the templ-components v2 migration or when adminui's SidebarNav adoption has proven the theming tokens in production.
- [~] **Wire remaining `check-*` apps into CI.** All gates except `check-cqrs-lint` run in CI (docs-links, service-methods, domain-counts, large-files, require-tags, codegen, templates, go-toolchain, annotation + row status gates + their self-tests since 2026-09-22, release-train BLOCKING with `--strict-lag 0` since 2026-09-09). Remaining: `check-cqrs-lint` (blocked: Nix-only binary; needs Go-installable distribution — see P3). `bench-spike` stays LOCAL-only by decision (machine-pinned baseline; see docs/benchmarks/README.md).

- [ ] **Re-enable BuildFlow `go-version-auto-configure` only after upstream fixes (SKIPPED 2026-09-20 — see the AGENTS.md gotcha).** The step corrupted the tree in commit `27057e5c`: it normalized the deliberate `go 1.27.1` floor to `go 1.27` (`go.work` + 4 module go.mods) and ran `go mod tidy` on the go.mod-only verify-tag fixtures, deleting every `require` line and vacuously passing `scripts/test-verify-tag.sh`. Blocked on two upstream changes in `go-version-auto-configure`: (1) prune `testdata` (and other fixture trees) from `surface.Discover`'s walk — it currently skips only `vendor/node_modules/.git/result`; (2) offer a policy knob for the major.minor `go`-directive rule (or align the repo's 1.27.1 floor). Also worth reporting to BuildFlow: a user `exclude:` list REPLACES `DefaultExcludePatterns` instead of merging (silently dropped `*_templ.go`, vendor, lock files — now mirrored explicitly in `.buildflow.yml`). Re-enable condition: both fixes shipped AND `buildflow --build-mode pre-commit --staged-only --dry-run` shows no fixture/directive churn.

- [ ] **`/sse` authz posture — RESOLVED 2026-09-21 (owner decision, design-tree interview): Option B shipped.** The session-gated full feed stays the documented default contract, and `setup.Config.SSEFilter` (new, additive) threads `transport.WithSSEFilter` into the shared SSEPath handler — live + replay scoped, fail-closed (`TestBundle_SSEHandlerFilterScopesReplay`; guide recipe in `docs/guides/sse-and-datastar.md` §Scoped Feeds; one-pager marked RESOLVED: `docs/planning/2026-08-30_sse-endpoint-shape-decision.md`). Per-user streams (C) stay demand-gated; D rejected.

- [ ] **DataStar Tier 4 — demand-gated panel variants (ADR-0050 ACCEPTED 2026-09-07; Tiers 1-3 SHIPPED, see CHANGELOG).** Strategy ADR: `docs/adr/0050-dual-frontend-protocol-strategy.md` (route-split accepted, Model B rejected, setup opt-in dependency decided, `/ds/events` session-gated 401, sequenced vs the open `/sse` decision by adoption). Shipped: `setup.Config.DataStarPath`/`DataStarScriptPath` + `Bundle.DataStarBroadcaster` (shared hub with `/sse`), route-split recipe in `fullstack-wiring.md` with a passing contract test (`integration_test/datastar_route_split_test.go`), setup-demo dual-transport showcase (`/ds-demo`, `POST /broadcast` fans out to both transports). REMAINING (each gated on consumer demand evidence per ADR-0050 §Decision 1 — plan Tier 4: `docs/planning/2026-09-07_17-18_datastar-dual-frontend-rollout.html`): (M11) dashboardui signal-patch spike + go/no-go; (M12) dashboardui DataStar variant module; (M13) loginpage signal forms; (M14) offline-sync evaluation vs sync-worker.js; (M15) adminui variant design doc; (M16) release train for the changed modules (setup carries the new API; its publish is part of the next family train regardless of Tier 4).

- [ ] **Kill the release-train advisory/strict split brain (CI red on 2026-09-22 was 100% preventable).** Local `check-release-train.sh` exits 0 on train lag while CI enforces `--strict-lag 0`; a session read "40 lag", pushed, and CI went red. Fix direction (owner-visible change, hence TODO not unilateral): default the local script to CI's exact flags or add a `--ci` preset, and consider a pre-push hook running release-train (CI flags) + version-drift --strict — the two blocking CI gates. Wave-ordered train choreography (every commit's requires point at already-published tags) made tag-day `--no-verify` unnecessary in v4.12.0 — codify it in the release runbook alongside. Source: `docs/status/2026-09-22_01-18_round6-tail-brutal-self-review-and-v4.12.0-train-status.md` §d1/§e1/§e7.
- [ ] **Post-train consumer-eye verification for v4.12.0.** (1) Throwaway module outside the workspace: `go get github.com/larsartmann/cqrs-htmx/v4@v4.12.0` (+ setup) and compile a minimal consumer — proves proxy propagation from the consumer side (integration_test covers the tagged content, but from inside the family). (2) Spot-check pkg.go.dev renders v4.12.0. Source: `docs/status/2026-09-22_01-18_round6-tail…` §c4.
- [ ] **Self-tests for the two gate scripts that bit us.** (1) `check-dep-budgets.sh` comment-line exclusion (fixed 2026-09-22; nothing prevents regression — scripts/test-* pattern exists); (2) VCS-cache health: wire the go-datastar no-remote sweep (`git -C <modcache-path> remote -v` empty ⇒ GOPRIVATE broke git resolution) into `check-modules` or the prewarm script. Source: round6-tail §e5, §f10/f11.
- [ ] **Snapshot the cqrs-lint zero-warning state and consider making stale-suppression warnings blocking.** All 14 modules `--strict` with zero warnings (2026-09-22, first green on 4.11.x after three root causes fixed — flake app toolchain pin, VCS corruption, finding hygiene). Stale-suppression warnings are free signal; blocking on them in CI is the natural next step once cqrs-lint is CI-installable. Source: round6-tail §f22.
- [ ] **Sweep `X-Client-Id` raw literals.** The v4.12.0 auth hardening introduced the header; use the exported constant everywhere (grep for raw `X-Client-Id` strings in root/usermgmt/setup). Source: round6-tail §f39.

---

## P3 — Technical debt & future

- [~] **Migrate usermgmt off go-cqrs-lite v5-removed APIs (V007 findings, re-surfaced 2026-09-20 via `cqrs-upgrade -dry-run --workspace` — one command now, the 22-manual-run era ended).** 78 findings (the tool flags per-identifier now, up from 39 site-level): 68 `storage.SQLViewStore`-family → metaengine engines with layout planning (ADR-0126); 6 `stack.Materialize` → metaengine auto-projection (ADR-0123); 4 `stack.Bundle` → `system.New` composition (ADR-0123 — the systemadapter module demonstrates the path). **Clusters (2)+(3) DEPRECATED 2026-09-21 (ADR-0051):** `MaterializeProjection`/`NewMaterializeProjection`/`.Materialize()`, the `EventSourcedSetup.Bundle` field, and the three SQL setup templates carry `// Deprecated:` markers — the spike found `NewEventSourcedSetup` already stack-free, so clusters 2+3 were deprecation-marking, not migration. Remaining: cluster (1) (the 68 SQLViewStore findings), gated on metaengine layout-planning covering secondary-index semantics + declarative hydration (ADR-0051 go-criterion). Companion: go-cqrs-lite will remove `stack/v4` entirely in v5 (owner decision 2026-09-21, recorded in that repo's ROADMAP).
- [ ] **Upstream asks (go-cqrs-lite) — (3)+(4) RECORDED 2026-09-22 (D1) in that repo's TODO_LIST** ("Upstream asks from cqrs-htmx, harvested 2026-09-22"), together with a third ask: `System.Explain` per-query Volume/placement display (verified absent empirically).** (1) ~~Decouple `stack/v4`'s root package from `metaengine/v4`~~ REDECIDED 2026-09-21 (user decision, design-tree interview): **remove `stack/v4` entirely in go-cqrs-lite v5** instead of decoupling it — metaengine + `system/` is the intended direction, the coupling is direction-aligned, and cqrs-htmx's V007 migration deletes the `stack.*` usage anyway (making the metaengine require intentional). Recorded in go-cqrs-lite's ROADMAP (verified surviving 2026-09-22). ((2) RESOLVED: `cqrs-upgrade --workspace` shipped upstream and was used for the 2026-09-20 train-lag sweep.)
- [ ] **Strip the `usermgmt` DEV-ONLY replace at the next family train.** `usermgmt/go.mod` carries `replace github.com/larsartmann/cqrs-htmx/v4 => ../` (added 2026-09-20) because `audit_context.go`'s `requestContextEnricher` uses `cqrshtmx.IPAddressFromContext`/`UserAgentFromContext`, and published root v4.11.0 lacks those symbols. Removal condition: the next train tags a root version carrying them. The replace is exempted by `check-release-train` (replace-satisfied requires) — but it makes hermetic `GOWORK=off` builds resolve root locally, so do not tag usermgmt before stripping it.
- [~] **Adopt appkit as the `setup` server layer (verdict consolidated in ADR-0052, 2026-09-22; Option A confirmed by spike, (b)–(f) assessed 2026-09-20 — all defer with rationale recorded in the ADR).** `RunWithAppkit` is stable since 2026-09-07 (go-appkit v0.5.x, no replace; SSE-flush/readiness/parity tests race-green). Remaining movement: only the v5-window revisit condition in ADR-0052 (health-dedup guidance + chain layering settled); nothing actionable before then.
- [ ] **Add cqrs-lint strict CI gate to GitHub Actions (same work as the P2 "wire remaining check-* apps" item — merged in the 2026-09-20 Pareto plan).** Run cqrs-lint `--strict` in CI to catch catalog/validation findings early. Blocked: cqrs-lint is a Nix-only binary; needs a Go-installable distribution or a Nix CI runner. The flake.nix `check-cqrs-lint` app exists for local use.
- [ ] **Hardware watch: `/mnt/buildcache` has a failure history.** The mount died 2026-08-16→2026-08-22 (every gate failing uniformly with "failed to initialize build cache") and was later repaired — as of 2026-09-15 it is healthy again (sdb1, 220G, build-verified). If it dies again: repoint `GOCACHE`/`GOMODCACHE`/`GOLANGCI_LINT_CACHE` to `/tmp` (mkdir first; prefix `git commit` too so the pre-commit hook inherits them; `scripts/lib/go-cache-env.sh` has the low-disk guard and fallback), and check `df -h /mnt/buildcache` first when gates fail en masse. Needs hardware attention if it recurs; NOT fixable from this repo.
- [ ] **SSE hardening backlog (from docs/status/archived/2026-08-21_21-39 §f).** ~~SSEMaxReplay input validation~~ DONE 2026-09-10 (setup `validate()` rejects negative values). Remaining (genuinely optional — route individually on demand): `transport/serve_test.go` gaps (replay-before-subscribe ordering, heartbeat join-on-exit, broadcaster-close-mid-stream, concurrent clients); `WithSSEMaxReplay` handler option; `SSEOptions` shared struct; `Retry` field on replayed domain events; `JournalSSEStore.EventsAfter` bench on large journals; fuzz `DomainEventToSSE`; e2e `/sse` reconnect-with-replay scenario; cross-module identical-wire-format contract test.
- [ ] **Remove `ProjectionLayer` in the v5 removal bundle (all prep DONE 2026-09-15).** `// Deprecated:` marker shipped (systemadapter/projections.go:99); migration guide `docs/guides/declarative-projections.md` (API mapping, drain semantics, checkpoint-store caveat); `examples/system-demo` migrated off `NewProjectionLayer` (now uses `sys.Start` + declarative queries); SQLite-engine declarative tests (`TestDeclarative_SQLite_UserLifecycle`, `TestDeclarative_SQLite_AuthzPolicies`) + negative-path tests (`TestDeclarative_MissingLookups`) landed; adminui/dashboardui verified ProjectionLayer-free; test migration (plan step 5.3) is covered by the declarative suite + `TestDeclarative_EquivalenceWithProjectionLayer` (the remaining ProjectionLayer tests legitimately test the deprecated layer itself). The former "one honest gap" is RESOLVED 2026-09-20: accepted-limitation note recorded in `docs/guides/v5-removal-inventory.md` — the declarative path has no custom checkpoint/DLQ injection (`system.New` uses an internal in-memory checkpoint store), so consumers needing durable checkpoints or dead letters stay on `NewProjectionLayer` until an upstream `system.New` option; if none lands before v5, removal proceeds with the limitation documented.
- [ ] **Decide `examples/datastar-demo` rebrand-or-remove** (modularization assessment 2026-07-19 item 5). Owner call. (`setup.NewFromSystem()` bridge: routed to ROADMAP Open Question 12 — build-or-reject decision lives there.)

---

_For completed work, see [CHANGELOG.md](CHANGELOG.md) and [git log](https://github.com/larsartmann/cqrs-htmx/commits/master). For long-term vision, v5 plans, and rejected ideas, see [ROADMAP.md](ROADMAP.md)._
