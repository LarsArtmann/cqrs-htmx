# TODO List — cqrs-htmx

> Short-term, actionable, bounded work. Open items only.
> Completed work lives in [CHANGELOG.md](CHANGELOG.md). Long-term vision, v5 plans, and rejected ideas live in [ROADMAP.md](ROADMAP.md).

**Updated:** 2026-08-29 | **Version:** v4.7.0 (released 2026-08-07) + `[Unreleased]` (async projection startup + ADR-0048, ActorID consolidation/ADR-0111, ADR-0114 tombstone migration, MySQL event-store completion, security middleware consolidation, httputil v0.11.0, Broadcaster Raw() accessor, fullstack UI integration tests, systemadapter module + system/metaengine integration, hermetic-build repair, new health/v4 + auditlog/v4 bridge modules, coordinated v4.8.0 family release train (PUSHED 2026-08-14), composability rounds 2-3: Service adoption seam + shared `/sse` with replay/backfill/heartbeat + canonical `transport` SSE envelope, **adoption benchmark hardened + spike tests 8.2s → 2.3s + setup_test.go 4-way split**, dashboardui SSE handler field→method promotion, dashboardui SSE heartbeat goroutine leak fix, v4.8.1 release: Go 1.26.7 + go-sse v0.5.1 + bench-spike gate) | **Modules:** 27 in `go.work` (15 production + 11 examples + e2e/server) | **`*Service` methods:** 73 (leading v5 indicator; see ROADMAP) | **Coverage (verified 2026-08-29, all gates green):** Root 93.3% (gate 90%), usermgmt 81.9% (gate 74%), identity-model 75.5% (gate 70%), dashboardui 83.5% + core 86.1% (gates 60/80), datastar 97.5% (gate 90%), setup 86.6% (gate 80%), systemadapter 89.6% (gate 70%), health 100% (gate 90), auditlog 100% (gate 90), adminui 68.5% (gate 66), loginpage 79.9% (gate 79) | **Lint:** 0 golangci issues across 15 modules (re-verified 2026-08-29); cqrs-lint strict passes all module configs. | **Full gate sweep green 2026-08-29:** test, race, coverage, coverage-gate, lint. | **✅ v4.8.0 family PUSHED (2026-08-14):** root, identity-model, usermgmt, webauthn, loginpage, dashboardui, adminui, setup (first), health (first), auditlog (first) — master + all 10 tags on origin; family dev replaces stripped, `check-modules` (isolation + relative-paths + strict version-drift) fully green. Remaining upstream asks: go-cqrs-lite projectionadapter/sqliteengine + go-datastar static/v0.2.0 (see `docs/runbooks/release-v4.8.0-push-plan.md` §3). | **appkit wave READY (2026-08-16):** go-appkit core v0.3.0 + cqrs/v0.3.0 + realtime/v0.1.0 + flightrecorder/v0.1.0 cut at `f938d65`, 6-module hermetic verify green, push pending (user gate) — ADR-001 fold-in unblocks the moment it ships (see go-appkit TODO_LIST P1).

## Status Legend

- [ ] **OPEN** — actionable, not yet started.
- [~] **PARTIALLY DONE** — started but incomplete.

> No `[x]` items here. When a task finishes, it moves to [CHANGELOG.md](CHANGELOG.md) and is removed from this list. Deferred/rejected ideas move to [ROADMAP.md](ROADMAP.md) → "Not Planned".

---

## P1 — High impact (release follow-through & doc health)

- [ ] **Cut the upstream tags that unblock the remaining trains (runbook §3, other repos, user's call).** REMAINING: go-cqrs-lite `metaengine/projectionadapter/v4 v4.5.0` + `metaengine/sqliteengine/v4 v4.0.2` (strip their workspace replaces in the tag commits; the repo's 2026-08-15 ADR-0128 extraction commit `5127039da` or later is a valid base — re-validate the runbook §3 mapping against that tree first, it now requires external `go-codec`/`go-retry`) → then `systemadapter/v4.8.0` + strip its metaengine replaces (and the matching one in `examples/system-demo`). DONE 2026-08-15: go-datastar `static/v0.2.0` tagged; `datastar/v4.8.0` cut, pushed, and proxy-resolvable; the 2 datastar-local family replaces stripped (`b5663b2b`); check-templates wired into CI (`e7aeee97`).
- [ ] **go-cqrs-lite upstream repairs blocking both build modes (2026-08-16, other repo, user's call).** (a) ~~Published `storage/v4 v4.7.0` does not compile~~ **DONE 2026-08-16:** upstream shipped `storage/v4 v4.7.1` (retracting v4.7.0); all 11 cqrs-htmx modules bumped to v4.7.1, suppression comments removed, hermetic GOWORK=off builds green. (b) ~~Sibling master `metaengine/planner.go:137` calls `newIdempotencyTracker()` against an `int`-taking signature~~ **FIXED UPSTREAM (verified 2026-08-29):** the call now passes `cfg.idempotencyCapacity`, `go build ./metaengine/...` is green on sibling master, and cqrs-htmx workspace-mode builds are green again. Both halves resolved — item can close when (a)'s follow-through is confirmed in the next train's notes.
- [ ] **~~Collapse setup's mirrored ServiceConfig subset into one source of truth.~~ RESOLVED 2026-08-29 (see CHANGELOG):** `setup.Config.ServiceConfig *usermgmt.ServiceConfig` is the escape hatch — precedence `Service` > `ServiceConfig` > flattened fields, conflicts rejected at `New`. The 10 flattened fields stay as the convenience path; embedding was rejected because it breaks every consumer struct literal. Any FUTURE ServiceConfig capability reaches setup with zero setup-side changes, closing the "add to setup too?" recurrence. Source: composability round-2 review, finding 4.

---

## P2 — Medium impact (tooling & quality)

- [~] **Wire remaining `check-*` apps into CI.** `check-docs-links`, `check-service-methods`, `check-domain-counts`, `check-large-files`, `check-require-tags` (renamed from `check-phantom-version` 2026-08-30; the CI step is the inline zero-pseudo grep), `check-codegen` (templ pinned @v0.3.1020), and `check-templates` (hermetic GOWORK=off rework + CI step with clean-restore assertion, 2026-08-15) now run in CI. Remaining: `check-cqrs-lint` (blocked: Nix-only binary; needs Go-installable distribution — see P3.1). New modules health + auditlog are wired into CI build/test/coverage/lint/mod-tidy (2026-08-14). DECIDED (2026-08-29): `bench-spike` stays LOCAL-only — the raw baseline is machine-pinned and CI runners are a different machine population (env guard exits 2 by design); see docs/benchmarks/README.md (annotation comment added to ci.yml 2026-08-30). `check-release-train` runs ADVISORY in CI since 2026-08-30 (`|| true`; the script also self-downgrades to WARN when ls-remote fails) — flip to blocking once ls-remote auth for the larsartmann repos is verified on an Actions runner; it is wired into `check-modules` locally and hard-fails on UNPUBLISHED requires. `check-go-toolchain` is BLOCKING in CI (2026-08-30, hermetic).

- [ ] **Decisions awaiting the user (2026-08-16 status report §g):** (1) family tag v4.8.1/v4.9.0 timing — publishes `transport.DomainEventToSSE`/`EventPayload` + setup replay/heartbeat + the dashboardui migration + the `ServiceConfig` escape hatch, and unblocks stripping 3 family dev-replaces (setup ×2, dashboardui ×1) plus the 3 dev-replaces in `examples/setup-demo` (setup + usermgmt + dashboardui). REMINDER for whichever train ships next: it MUST tag `usermgmt/totp` (remote max v4.7.0 — the never-published `totp/v4.8.0` require in examples/admin-demo poisoned the entire workspace module graph; pinned back to v4.7.0 on 2026-08-29, see CHANGELOG) and `usermgmt/oauth2` (also has no v4.8.x tag), and push the locally-cut `dashboardui/v4.8.1` + `datastar/v4.8.1`; (2) `/sse` authz posture — session-gating vs adding a stream-type filter before the endpoint shape is published (see `sse.ReplayFiltered` as a basis). RESOLVED: (3) Go toolchain — v4.8.1 shipped on 1.26.7 (2026-08-29); NOTE: CORS posture decided (2026-08-21) — same-origin default, consumer wraps with `httputil.CORS`; documented in `docs/guides/sse-and-datastar.md`.
- [ ] **~~Commit cqrs-htmx `go.work.sum` drift deterministically~~ RESOLVED 2026-08-30 (see CHANGELOG):** `go.work.sum` is now fully untracked — it was gitignored all along but still indexed, so the auto-git daemon kept absorbing its drift into unrelated commits (e.g. `0e2b0aa3`). Workspace go commands regenerate it on demand; workspace build verified green after removal.

---

## P3 — Technical debt & future

- [ ] **~~Add a non-trivial-handler sub-benchmark...~~ RESOLVED 2026-08-29:** `BenchmarkSpikeBaselineVsAppkit/json-roundtrip` isolates the JSON codec cost of an envelope-shaped payload so stack deltas attribute to middleware rather than codec work; baseline re-pinned (`--save-baseline`) in the same change. Source: bench-spike session §f.15.
- [ ] **Upstream ask (go-cqrs-lite): decouple `stack/v4`'s root package from `metaengine/v4`.** Re-scoped 2026-08-29 after tracing the former "drop the stale metaengine/v4 require from usermgmt/go.mod" item — removal from usermgmt's side is NOT possible: usermgmt imports `stack/v4` (es_materialize_adapter.go, es_setup_core.go, stack_repositories.go, sqlite_setup.go), and stack v4.3.0's own root-package files (`options.go`, `bundle.go`) import metaengine packages, so metaengine is a genuine transitive package dependency and `go mod tidy` correctly keeps the `// indirect` require. The 2026-08-16 premise ("no Go file under usermgmt imports metaengine") was true but irrelevant — package imports by dependencies are the edge. Only an upstream split of stack's option/bundle layer from metaengine removes it. Source: composability round-2 review, finding 6 + this trace.
- [ ] **Adopt appkit as the `setup` server layer (ADR-001 Option A confirmed by spike).** The `spike/appkit-server` branch carries `RunWithAppkit` + passing SSE-flush/readiness/parity tests and the adopt report (`docs/status/2026-08-16_01-38_appkit-spike-adopt.md`): hardened benchmark (5× runs, `-benchmem`, `LogLevelError` to isolate stack from logging — see CHANGELOG) shows baseline-httputil ~17µs/op @ ~60k req/s vs appkit-service ~20µs/op @ ~50k req/s, ~30 extra allocs/op per request being the real cost; drain-phase and projection-aware readiness both verified under `-race`. **READY (2026-08-16):** go-appkit core v0.3.0 (`NoTimeout` + `ReadyCheck`) is cut at `f938d65` together with cqrs/v0.3.0, realtime/v0.1.0, flightrecorder/v0.1.0 — push is the only remaining step (user gate; see go-appkit TODO_LIST P1). Fold-in must resolve in ONE change: (a) drop the spike `replace`, point `go-appkit` at v0.3.0; (b) fold `RunWithAppkit` into `RunHandler` behind the unchanged signature; (c) `/health` semantics — suppress the bundle's duplicate `/health` when appkit owns health, document that LBs must target `/health/ready` (comparison finding 2: the meaning silently flips readiness→liveness in appkit mode); (d) dedupe the stacked chains — drop bundle recovery (appkit's is outermost) and the duplicate security-header pass (finding 3); (e) decide the logging posture — wire setup's logger or configure the appkit level; the 2.8x bench delta in the spike was entirely per-request INFO logging (finding 7 — now isolated by `LogLevelError` in the hardened bench); (f) consider exposing `Addr()` (new capability). Sources: comparison report `docs/review/2026-08-16_setup-vs-go-appkit-comparison.md` §4/§6/§8 (findings 1-3, 7).
- [ ] **Add cqrs-lint strict CI gate to GitHub Actions.** Run cqrs-lint `--strict` in CI to catch catalog/validation findings early. Blocked: cqrs-lint is a Nix-only binary; needs a Go-installable distribution or a Nix CI runner. The flake.nix `check-cqrs-lint` app exists for local use.
- [ ] **Cross-module dep version drift after v4.7.0 tagging.** Published submodule tags reference stale sibling versions (e.g., `adminui/v4.7.0` still depends on `usermgmt/v4.6.1`). Next release should bump all cross-module refs before tagging. Source: `docs/status/archived/2026-08-07_22-23_release-v4.7.0-and-retry-investigation.md`. CURRENT SPLITS (2026-08-29, making `check-modules` strict drift red until the pending train): `cqrs-htmx/usermgmt/v4` at v4.8.0 (most modules) vs v4.8.1 (setup AND examples/setup-demo — tag not yet cut/pushed remotely); `record/v4` at v4.3.0 (15 modules) vs v4.4.0 (systemadapter — forced by its documented TEMPORARY metaengine replace; sibling master needs v4.4.0, hermetic build breaks without it); `metaengine/v4` v4.11.0 vs local master (same documented TEMPORARY replace). All three dissolve in the next family train (tag + push `usermgmt/v4.8.x`, strip the TEMPORARY replaces, align refs to the family version). Stage status on 2026-08-30 HEAD (`check-modules` run end-to-end): isolation ✅, dep-budgets ✅, go-toolchain ✅, drift --strict ✗ (this item), release-train ✅ (0 unpublished / 3 replace-exempted / 367 lag), replace-directives ✅, docs-freshness ✅, docs-links ✅.
- [ ] **Re-investigate datastar/go-sse architecture decision.** The prior analysis (`docs/status/archived/2026-08-07_06-25_datastar-go-sse-analysis-self-review.md`) claimed go-sse cannot produce Datastar wire format — but go-sse has `KeyedLines`/`SendKeyed`/`SendLines` designed for Datastar. The exclusion is a design choice (Patch coupling to SDK), not a technical incompatibility. Needs either an ADR documenting the decision or a migration to go-sse.
- [~] **Add golines alignment to `nix fmt` pipeline.** MEASURED 2026-08-29 (nixpkgs `golines-0.15.0`, treefmt-nix programs.golines): first `nix fmt` run reformats **275 files** — treefmt golines defaults disagree with the golangci-lint `golines` settings that the lint gate has been enforcing (plus shfmt+shellcheck flag legacy gate scripts), so enabling it silently would bury unrelated work under reformat churn. Path forward: a DEDICATED format-sweep commit (align treefmt golines config with the golangci config first, triage shfmt/shellcheck script fallout), then enable in treefmt. Reverted same day — no gate change shipped.
- [ ] **Consider a Go-based markdown link checker.** The current `check-docs-links.sh` uses awk regex which handles common cases but may miss edge cases. A Go checker using goldmark would be more robust. The awk checker + test suite is sufficient for now.
- [ ] **Rewrite `origin/v4` branch history to strip 3 remaining binary blobs (~27.7 MB).** The master branch was cleaned via `git filter-repo` (731.8 MB → 0 blobs), but the `v4` branch has independent binary contamination (`examples/basic/basic` 9.8MB, `examples/datastar-demo/datastar-demo` 8.9MB ×2) that does not share ancestry with master. Requires `git filter-repo` on the v4 branch + force-push. Source: `docs/status/2026-08-09_06-15_git-binary-cleanup-history-rewrite.md`.
- [ ] **Purge `examples/setup-demo/setup-demo` (27 MB) from pushed master history.** Removed from HEAD and from all unpushed commits 2026-08-14 (filter-branch; family tags re-pointed), but the blob still exists in already-pushed history (introduced `5604e810`, last carried by `73ff1556`). Requires filter-repo + force-push on master — same class as the v4 branch item above. Low priority: the repo already survived a 731.8 MB cleanup.
- [ ] **Hardware: `/mnt/buildcache` (sda1) is FULLY DEAD since 2026-08-22** — began failing 2026-08-16 with I/O errors, now the mount is gone entirely (`df` reports "No such device"); `GOCACHE`/`GOMODCACHE`/`GOLANGCI_LINT_CACHE` pointed there, so every gate fails uniformly with `failed to initialize build cache`. The documented `$HOME/.cache` workaround ALSO no longer works (`~/.cache/go-build` is a read-only nix-store symlink). Current working workaround: `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOLANGCI_LINT_CACHE=/tmp/golangci-cache` (mkdir first; prefix `git commit` too so the pre-commit hook inherits them; cold /tmp module cache re-downloads deps). Root FS (nvme) healthy. Needs hardware attention; NOT fixable from this repo.

---

_For completed work, see [CHANGELOG.md](CHANGELOG.md) and [git log](https://github.com/larsartmann/cqrs-htmx/commits/master). For long-term vision, v5 plans, and rejected ideas, see [ROADMAP.md](ROADMAP.md)._
