# TODO List — cqrs-htmx

> Short-term, actionable, bounded work. Open items only.
> Completed work lives in [CHANGELOG.md](CHANGELOG.md). Long-term vision, v5 plans, and rejected ideas live in [ROADMAP.md](ROADMAP.md).

**Updated:** 2026-09-20 (round 5) | **Rounds 1–5 of the all-TODO execution landed:** routed-gaps bundle (IP/UA/ClientID metadata, DecodePayload re-exports, DecodePaginationStrict, `Retry-After` on 503), SSE hardening backlog complete (handler-level `WithSSEMaxReplay` + `SSEOptions` + replayed `retry:` hint + contract tests), findings gate `fail_on: critical` (2026-09-18), adminui prettier-gap program COMPLETE (Phases 0–4 incl. shared confirm modal, identity dropdown, `PolledRegion` stats, errorpage, dark sweep, mobile-hamburger fix; 45/45 browser checks + 9/9 checked-in Playwright specs), systemadapter first-ever tag (`v4.11.0`, 2026-09-20), train-lag swept to ZERO | **Version:** v4.11.0 family train (2026-09-19) | **Modules:** 28 in `go.work` | **Coverage:** all 15 gates green (`nix run .#coverage-gate` 2026-09-20) | **Lint:** 0 issues / 15 modules

## Status Legend

- [ ] **OPEN** — actionable, not yet started.
- [~] **PARTIALLY DONE** — started but incomplete.

> No `[x]` items here. When a task finishes, it moves to [CHANGELOG.md](CHANGELOG.md) and is removed from this list. Deferred/rejected ideas move to [ROADMAP.md](ROADMAP.md) → "Not Planned".

---

## P1 — High impact (release follow-through & doc health)

- [ ] **bench-spike idle re-run (sole remainder of the post-bump deferred verification pass).** Everything else in the 2026-09-10 pass is green (coverage-gate 15/15, test-race, test-fuzz, test-flake, check-cqrs-lint, e2e 4/4 + admin-behavior 9/9). The v4.9.0 train swapped go-appkit resolution (replace → published v0.4.0), so the appkit-service sub-bench legitimately measures a different dependency resolution: on an IDLE machine, re-run `nix run .#bench-spike`; if appkit-service alone exceeds the 10% gate with the other two green, re-pin via `--save-baseline` in the same change and commit `docs/benchmarks/setup-baseline.raw.txt`. Never re-pin under load — refused 2026-09-15 (load 825/32), 2026-09-17 (load 16.4 vs limit 8), 2026-09-20 morning (load 72 vs limit 8), and 2026-09-20 round 4 (load 19.9 vs limit 8 — 6th documented refusal; setup bench paths untouched this round: only adminui + examples/admin-demo changed). The gate's own `BENCH_MAX_LOAD` guard enforces this mechanically.

---

## P2 — Medium impact (tooling & quality)

- [~] **Command-audit session tail — post-train remainder (2026-09-19: sub-items 1–3 of the old checklist DONE in the v4.11.0 train — usermgmt tagged first, setup/integration_test requires bumped + family dev-replaces stripped, examples/basic manual-enrichment line dropped, check-release-train 0/0/1).** Remaining: (4) re-run `bench-spike` on an idle machine; if the 10% gate trips after the dispatch-audit chain (~275 ns/dispatch measured), re-pin per policy in the same change. (5) Consider upstreaming `requestContextEnricher` to go-cqrs-lite event/ so the local copy drops at the next train.
- [ ] **templ-components v1.19 train prep.** v1.18.0 landed repo-wide 2026-09-19 (uniform, integration_test included — the lag pattern ended). v1.19 (upstream Unreleased) makes Cards sharp by default (Card/SimpleCard/StatCard lose `rounded-lg`): when it ships, bump + rebuild BOTH CSS bundles + re-run the dashboardui screenshot pass (N5 harness) before tagging. Also upstream-ask: ListNote range semantics, Grid children doc (see N14 items below). NEW upstream candidates from the 2026-09-20 adminui round: (d) `DropdownProps` has no custom-trigger slot (Label is a string) — an avatar-in-trigger needs a `Trigger templ.Component` field; (e) the popover positioner registers `toggle` without capture — popover toggle does not bubble, so Dropdown/Popover panels render at the viewport corner (adminui carries a vendored capture-phase positioner in admin.js until fixed; verified empirically 2026-09-20).
- [ ] **templ-components upstream asks (file after verify-before-filing; local record kept here).** (a) `display.ListNote`: add a range/list semantics variant — consumers showing "N items in range" need count-only or per-item, never both; (b) `display.Grid`: document that `{children...}` is empty when the component is rendered standalone into a strings.Builder (hybrid path) — or support data-props children; (c) `display.CopyButton`: the inner `span[data-tc-copy-text]` inherits ancestor color in consumers whose unlayered table CSS overrides it — consider an explicit default color on the span. Upstream repo: github.com/larsartmann/templ-components.
- [ ] **adminui theme-toggle implementation ❓ (M089 sign-off gate).** Strategy spike written 2026-09-20 (`docs/research/2026-09-20_theme-toggle-strategy-spike.md`): Option B recommended — keep the token-flip architecture, add a `data-theme` attribute toggle (~30 lines: duplicate the dark token block under `:root[data-theme="dark"]`, ghost sun/moon button, three-state localStorage + pre-paint nonce script). Option A (class-strategy `dark:` migration) buys no user-visible capability for a full-rewrite diff. Awaiting the user's pick before ANY theme code (plan guardrail); auto mode renders byte-identically meanwhile.
- [ ] **SidebarNav revisit criteria (dashboardui).** Custom dark sidebar stays while ALL of: (1) it renders in the string-builder hybrid path without children-slot loss, (2) mobile drawer keeps zero-JS, (3) the library ships a dark-token shell matching `--sidebar-bg`/`--tc-sidebar-*` variables. Re-evaluate at the templ-components v2 migration or when adminui's SidebarNav adoption has proven the theming tokens in production.
- [~] **Wire remaining `check-*` apps into CI.** All gates except `check-cqrs-lint` run in CI (docs-links, service-methods, domain-counts, large-files, require-tags, codegen, templates, go-toolchain, release-train BLOCKING with `--strict-lag 0` since 2026-09-09 — the flip's first run caught a real templ-components drift). Remaining: `check-cqrs-lint` (blocked: Nix-only binary; needs Go-installable distribution — see P3). `bench-spike` stays LOCAL-only by decision (machine-pinned baseline; see docs/benchmarks/README.md).

- [ ] **Decision awaiting the user: `/sse` authz posture** — session-gating vs adding a stream-type filter before the endpoint shape is published. SPIKED + MECHANISM SHIPPED 2026-08-30 (`transport.WithSSEFilter` + fail-closed `filteredEventStore`; documented in `docs/guides/sse-and-datastar.md` §Scoped Feeds). Decision one-pager with options table + recommendation (A ship as-is, B scoped option, C per-user streams, D drop shared /sse): `docs/planning/2026-08-30_sse-endpoint-shape-decision.md`. Purely a product/security call now, not a feasibility question. (Former items (1) tag timing and (3) Go toolchain: RESOLVED — see CHANGELOG.)

- [ ] **DataStar Tier 4 — demand-gated panel variants (ADR-0050 ACCEPTED 2026-09-07; Tiers 1-3 SHIPPED, see CHANGELOG).** Strategy ADR: `docs/adr/0050-dual-frontend-protocol-strategy.md` (route-split accepted, Model B rejected, setup opt-in dependency decided, `/ds/events` session-gated 401, sequenced vs the open `/sse` decision by adoption). Shipped: `setup.Config.DataStarPath`/`DataStarScriptPath` + `Bundle.DataStarBroadcaster` (shared hub with `/sse`), route-split recipe in `fullstack-wiring.md` with a passing contract test (`integration_test/datastar_route_split_test.go`), setup-demo dual-transport showcase (`/ds-demo`, `POST /broadcast` fans out to both transports). REMAINING (each gated on consumer demand evidence per ADR-0050 §Decision 1 — plan Tier 4: `docs/planning/2026-09-07_17-18_datastar-dual-frontend-rollout.html`): (M11) dashboardui signal-patch spike + go/no-go; (M12) dashboardui DataStar variant module; (M13) loginpage signal forms; (M14) offline-sync evaluation vs sync-worker.js; (M15) adminui variant design doc; (M16) release train for the changed modules (setup carries the new API; its publish is part of the next family train regardless of Tier 4).

---

## P3 — Technical debt & future

- [ ] **Migrate usermgmt off go-cqrs-lite v5-removed APIs (V007 findings, re-surfaced 2026-09-20 via `cqrs-upgrade -dry-run --workspace` — one command now, the 22-manual-run era ended).** 78 findings (the tool flags per-identifier now, up from 39 site-level): 68 `storage.SQLViewStore`-family → metaengine engines with layout planning (ADR-0126); 6 `stack.Materialize` → metaengine auto-projection (ADR-0123); 4 `stack.Bundle` → `system.New` composition (ADR-0123 — the systemadapter module demonstrates the path). All still compile and test green on the v4.11.0 trains — v5-prep, not a v4 blocker. Sequencing: cluster (2) is post-systemadapter-tag; cluster (3) follows metaengine layout-planning API maturity.
- [ ] **Upstream asks (go-cqrs-lite).** (1) Decouple `stack/v4`'s root package from `metaengine/v4` — re-scoped 2026-08-29: removal from usermgmt's side is NOT possible; stack's own root-package files (`options.go`, `bundle.go`) import metaengine packages, so only an upstream split of stack's option/bundle layer removes the transitive require. Source: composability round-2 review, finding 6 + trace. ((2) RESOLVED: `cqrs-upgrade --workspace` shipped upstream and was used for the 2026-09-20 train-lag sweep.)
- [~] **Adopt appkit as the `setup` server layer (ADR-001 Option A confirmed by spike; (b)–(f) ASSESSED 2026-09-20 — all defer, rationale recorded).** `RunWithAppkit` is stable since 2026-09-07 (go-appkit v0.5.x, no replace; SSE-flush/readiness/parity tests race-green). Assessment of the ADR-001 default-flip follow-ups: **(b) fold into `RunHandler`** — defer to v5: it silently changes the DEFAULT path's behavior (2s drain delay, appkit's generic middleware wrapping, /health/* reshaped); needs (c)+(d) settled first and stays behind the DataStar ADR-first sequencing. **(c) `/health` dedup + LB guidance** — document, don't automate: appkit serves /health,/health/live,/health/ready while the bundle's session-gated HealthPath mount coexists by design; `HealthPath: "-"` is the existing opt-out, and Mount() cannot know which run path a consumer will call. **(d) stacked-chain dedup** — NOT safe to dedup blindly: the bundle's inner RecommendedSecurityMiddleware owns CSP NONCE generation (appkit's SecurityHeaders do not); the layering (generic outer, domain-aware inner) is already documented in run_appkit.go. **(e) logging posture** — the 2.8x bench delta is isolated by `LogLevelError` in the bench; an appkit LogLevel config seam is marginal (RunHandler stays the quiet path). **(f) expose `Addr()`** — small (listener is captured in appkit's Start) but racy to expose post-Start; do on consumer demand, same gate as DataStar Tier 4. Sources: comparison report `docs/review/2026-08-16_setup-vs-go-appkit-comparison.md` §4/§6/§8, adopt report `docs/status/archived/2026-08-16_01-38_appkit-spike-adopt.md`.
- [ ] **Add cqrs-lint strict CI gate to GitHub Actions (same work as the P2 "wire remaining check-* apps" item — merged in the 2026-09-20 Pareto plan).** Run cqrs-lint `--strict` in CI to catch catalog/validation findings early. Blocked: cqrs-lint is a Nix-only binary; needs a Go-installable distribution or a Nix CI runner. The flake.nix `check-cqrs-lint` app exists for local use.
- [ ] **Rewrite `origin/v4` branch history to strip 3 remaining binary blobs (~27.7 MB).** The master branch was cleaned via `git filter-repo` (731.8 MB → 0 blobs), but the `v4` branch has independent binary contamination (`examples/basic/basic` 9.8MB, `examples/datastar-demo/datastar-demo` 8.9MB ×2) that does not share ancestry with master. Requires `git filter-repo` on the v4 branch + force-push. Source: `docs/status/2026-08-09_06-15_git-binary-cleanup-history-rewrite.md`.
- [ ] **Purge `examples/setup-demo/setup-demo` (27 MB) from pushed master history.** Removed from HEAD and from all unpushed commits 2026-08-14 (filter-branch; family tags re-pointed), but the blob still exists in already-pushed history (introduced `5604e810`, last carried by `73ff1556`). Requires filter-repo + force-push on master — same class as the v4 branch item above. Low priority: the repo already survived a 731.8 MB cleanup.
- [ ] **Hardware watch: `/mnt/buildcache` has a failure history.** The mount died 2026-08-16→2026-08-22 (every gate failing uniformly with "failed to initialize build cache") and was later repaired — as of 2026-09-15 it is healthy again (sdb1, 220G, build-verified). If it dies again: repoint `GOCACHE`/`GOMODCACHE`/`GOLANGCI_LINT_CACHE` to `/tmp` (mkdir first; prefix `git commit` too so the pre-commit hook inherits them; `scripts/lib/go-cache-env.sh` has the low-disk guard and fallback), and check `df -h /mnt/buildcache` first when gates fail en masse. Needs hardware attention if it recurs; NOT fixable from this repo.
- [ ] **SSE hardening backlog (from docs/status/archived/2026-08-21_21-39 §f).** ~~SSEMaxReplay input validation~~ DONE 2026-09-10 (setup `validate()` rejects negative values). Remaining (genuinely optional — route individually on demand): `transport/serve_test.go` gaps (replay-before-subscribe ordering, heartbeat join-on-exit, broadcaster-close-mid-stream, concurrent clients); `WithSSEMaxReplay` handler option; `SSEOptions` shared struct; `Retry` field on replayed domain events; `JournalSSEStore.EventsAfter` bench on large journals; fuzz `DomainEventToSSE`; e2e `/sse` reconnect-with-replay scenario; cross-module identical-wire-format contract test.
- [ ] **Remove `ProjectionLayer` in the v5 removal bundle (all prep DONE 2026-09-15).** `// Deprecated:` marker shipped (systemadapter/projections.go:99); migration guide `docs/guides/declarative-projections.md` (API mapping, drain semantics, checkpoint-store caveat); `examples/system-demo` migrated off `NewProjectionLayer` (now uses `sys.Start` + declarative queries); SQLite-engine declarative tests (`TestDeclarative_SQLite_UserLifecycle`, `TestDeclarative_SQLite_AuthzPolicies`) + negative-path tests (`TestDeclarative_MissingLookups`) landed; adminui/dashboardui verified ProjectionLayer-free; test migration (plan step 5.3) is covered by the declarative suite + `TestDeclarative_EquivalenceWithProjectionLayer` (the remaining ProjectionLayer tests legitimately test the deprecated layer itself). The former "one honest gap" is RESOLVED 2026-09-20: accepted-limitation note recorded in `docs/guides/v5-removal-inventory.md` — the declarative path has no custom checkpoint/DLQ injection (`system.New` uses an internal in-memory checkpoint store), so consumers needing durable checkpoints or dead letters stay on `NewProjectionLayer` until an upstream `system.New` option; if none lands before v5, removal proceeds with the limitation documented.
- [ ] **Decide `examples/datastar-demo` rebrand-or-remove** (modularization assessment 2026-07-19 item 5). Owner call. (`setup.NewFromSystem()` bridge: routed to ROADMAP Open Question 12 — build-or-reject decision lives there.)

---

_For completed work, see [CHANGELOG.md](CHANGELOG.md) and [git log](https://github.com/larsartmann/cqrs-htmx/commits/master). For long-term vision, v5 plans, and rejected ideas, see [ROADMAP.md](ROADMAP.md)._
