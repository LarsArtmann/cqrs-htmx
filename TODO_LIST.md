# TODO List — cqrs-htmx

> Short-term, actionable, bounded work. Open items only.
> Completed work lives in [CHANGELOG.md](CHANGELOG.md). Long-term vision, v5 plans, and rejected ideas live in [ROADMAP.md](ROADMAP.md).

**Updated:** 2026-09-22 (post-adoption harvest) | **Round 8 (full-list execution):** CI-parity pre-push gate + strict-by-default release-train (split brain dead), dep-budgets + VCS-cache gates with self-tests (repo + flake + CI), v4.12.0 consumer-eye verification (throwaway module + pkg.go.dev), templ-components asks (a)–(c) implemented and CUT as v1.19.1 (+ red-master CI fixes; **both repos PUSHED 2026-09-22** — real-CI observation pending, P1), v1.19.1 adopted repo-wide (12 modules + both CSS bundles + dashboardui `ListNoteCount` under the DLQ table), cqrs-lint zero-warning state re-verified, SSE hardening backlog confirmed fully shipped in v4.12.0 (TODO was stale), X-Client-Id sweep closed | **Version:** v4.12.0 family train (14 tags, 2026-09-22, pushed; CI green) | **Modules:** 28 in `go.work` | **Coverage:** all 15 gates green (`nix run .#coverage-gate` 2026-09-22) | **Lint:** 0 issues / 15 modules

## Status Legend

- [ ] **OPEN** — actionable, not yet started.
- [~] **PARTIALLY DONE** — started but incomplete.

> No `[x]` items here. When a task finishes, it moves to [CHANGELOG.md](CHANGELOG.md) and is removed from this list. Deferred/rejected ideas move to [ROADMAP.md](ROADMAP.md) → "Not Planned".

---

## P1 — High impact (release follow-through & doc health)

- [ ] **bench-spike idle re-run (sole remainder of the post-bump deferred verification pass).** Everything else in the 2026-09-10 pass is green (coverage-gate 15/15, test-race, test-fuzz, test-flake, check-cqrs-lint, e2e 4/4 + admin-behavior 9/9). The v4.9.0 train swapped go-appkit resolution (replace → published v0.4.0), so the appkit-service sub-bench legitimately measures a different dependency resolution: on an IDLE machine, re-run `nix run .#bench-spike`; if appkit-service alone exceeds the 10% gate with the other two green, re-pin via `--save-baseline` in the same change and commit `docs/benchmarks/setup-baseline.raw.txt`. Never re-pin under load — refused 2026-09-15 (load 825/32), 2026-09-17 (load 16.4), 2026-09-20 ×2 (72 and 19.9), twice during the 2026-09-21/22 train, and 2026-09-22 round-8 (load 614 falling to 36 vs limit 8 — 9 documented refusals; external workloads on 32 cores). The gate's own `BENCH_MAX_LOAD` guard enforces this mechanically. **Structural question (owner): automate or retire** — see ROADMAP Open Questions (OQ16).

- [ ] **Watch CI on both repos' pushed ranges; fix forward same-day if red.** cqrs-htmx `cbf3fdfb..5f1d1bca` (12-module templ-components v1.19.1 bump `eb6dfc7a`, both CSS bundles `2f59832e`, dashboardui ListNoteCount adoption `14e386e3`+`650404e1`, docs) and templ-components master + the 7 v1.19.1 tags (root `f917cb89` — the pushed range carries the three red-master CI fixes, which real CI has NOT yet executed). Both pushes landed 2026-09-22 via the release-train session's sync completion (`docs/status/2026-09-22_13-25_release-train-alignment-tc-v1-19-1-push-unblocked.md` (a)10); local gates were green, but "local green" has historically diverged from CI. Also verify pkg.go.dev renders v1.19.1. (Sources: adoption-session report §f1–2; release-train report §b2, §b5.)

---

## P2 — Medium impact (tooling & quality)

- [ ] **SidebarNav revisit criteria (dashboardui).** Custom dark sidebar stays while ALL of: (1) it renders in the string-builder hybrid path without children-slot loss, (2) mobile drawer keeps zero-JS, (3) the library ships a dark-token shell matching `--sidebar-bg`/`--tc-sidebar-*` variables. Re-evaluate at the templ-components v2 migration or when adminui's SidebarNav adoption has proven the theming tokens in production. NOTE (2026-09-22): the hybrid-path children question now has a documented escape hatch upstream — `templ.WithChildren(ctx, child)` populates children slots in strings.Builder rendering (templ-components v1.19.1 recipe `docs/recipes/hybrid-strings-builder-rendering.md`, pinned by tests); criterion (1) is now satisfiable in principle.
- [~] **Wire remaining `check-*` apps into CI.** All gates except `check-cqrs-lint` run in CI (docs-links, service-methods, domain-counts, large-files, require-tags, codegen, templates, go-toolchain, annotation + row status gates + their self-tests, dep-budgets self-test + VCS-cache health + self-test since 2026-09-22 round-8, release-train BLOCKING with `--strict-lag 0` since 2026-09-09). Remaining: `check-cqrs-lint` (blocked: Nix-only binary; needs Go-installable distribution — see P3). `bench-spike` stays LOCAL-only by decision (machine-pinned baseline; see docs/benchmarks/README.md).

- [ ] **CSS-bundle sanity guard + staged-state producer hunt (2026-09-22 ~13:05 incident).** The git index briefly held an ~8KB-deleted (near-empty) dashboardui bundle while HEAD had the 104KB form; the producer was never identified (suspect: a buildflow pre-commit/tailwind step running with an empty content scan). Part 1: reproduce/hunt the producer. Part 2: mechanical guard — committed bundles must be the canonical MINIFIED builder output (1 line / expected byte range / canary strings), wired into pre-commit or check-modules, atomic per the gate checklist (checker + fixture self-test + flake app + CI + README). Until guarded, any family bump can re-trigger the class. (Source: release-train report §b3, §f1–2.)
- [ ] **e2e: DLQ count-notice assertion + full Playwright run post-adoption.** The DLQ footer ("Showing N items." + `aria-label="Dead letter count"`, `dashboardui/handlers_dlq.go`) is pinned by golden + handler assertions only; extend `e2e/tests/dashboard.spec.ts` with an assertion and run the full suite (57/57 precedent). A human-grade look at the note (contrast + dark mode) rides along. (Sources: adoption-session report §b3, §c2, §e5, §f6–7.)

- [ ] **Re-enable BuildFlow `go-version-auto-configure` only after upstream fixes (SKIPPED 2026-09-20 — see the AGENTS.md gotcha).** The step corrupted the tree in commit `27057e5c`: it normalized the deliberate `go 1.27.1` floor to `go 1.27` (`go.work` + 4 module go.mods) and ran `go mod tidy` on the go.mod-only verify-tag fixtures, deleting every `require` line and vacuously passing `scripts/test-verify-tag.sh`. Blocked on two upstream changes in `go-version-auto-configure`: (1) prune `testdata` (and other fixture trees) from `surface.Discover`'s walk — it currently skips only `vendor/node_modules/.git/result`; (2) offer a policy knob for the major.minor `go`-directive rule (or align the repo's 1.27.1 floor). Also worth reporting to BuildFlow: a user `exclude:` list REPLACES `DefaultExcludePatterns` instead of merging (silently dropped `*_templ.go`, vendor, lock files — now mirrored explicitly in `.buildflow.yml`). Re-enable condition: both fixes shipped AND `buildflow --build-mode pre-commit --staged-only --dry-run` shows no fixture/directive churn.

- [ ] **DataStar Tier 4 — demand-gated panel variants (ADR-0050 ACCEPTED 2026-09-07; Tiers 1-3 SHIPPED, see CHANGELOG).** Strategy ADR: `docs/adr/0050-dual-frontend-protocol-strategy.md` (route-split accepted, Model B rejected, setup opt-in dependency decided, `/ds/events` session-gated 401, sequenced vs the open `/sse` decision by adoption). Shipped: `setup.Config.DataStarPath`/`DataStarScriptPath` + `Bundle.DataStarBroadcaster` (shared hub with `/sse`), route-split recipe in `fullstack-wiring.md` with a passing contract test (`integration_test/datastar_route_split_test.go`), setup-demo dual-transport showcase (`/ds-demo`, `POST /broadcast` fans out to both transports). REMAINING (each gated on consumer demand evidence per ADR-0050 §Decision 1 — plan Tier 4: `docs/planning/2026-09-07_17-18_datastar-dual-frontend-rollout.html`): (M11) dashboardui signal-patch spike + go/no-go; (M12) dashboardui DataStar variant module; (M13) loginpage signal forms; (M14) offline-sync evaluation vs sync-worker.js; (M15) adminui variant design doc; (M16) release train for the changed modules (setup carries the new API; its publish is part of the next family train regardless of Tier 4).

---

## P3 — Technical debt & future

- [~] **Migrate usermgmt off go-cqrs-lite v5-removed APIs (V007 findings, re-surfaced 2026-09-20 via `cqrs-upgrade -dry-run --workspace` — one command now, the 22-manual-run era ended).** 78 findings (the tool flags per-identifier now, up from 39 site-level): 68 `storage.SQLViewStore`-family → metaengine engines with layout planning (ADR-0126); 6 `stack.Materialize` → metaengine auto-projection (ADR-0123); 4 `stack.Bundle` → `system.New` composition (ADR-0123 — the systemadapter module demonstrates the path). **Clusters (2)+(3) DEPRECATED 2026-09-21 (ADR-0051):** `MaterializeProjection`/`NewMaterializeProjection`/`.Materialize()`, the `EventSourcedSetup.Bundle` field, and the three SQL setup templates carry `// Deprecated:` markers — the spike found `NewEventSourcedSetup` already stack-free, so clusters 2+3 were deprecation-marking, not migration. Remaining: cluster (1) (the 68 SQLViewStore findings), gated on metaengine layout-planning covering secondary-index semantics + declarative hydration (ADR-0051 go-criterion). Companion: go-cqrs-lite will remove `stack/v4` entirely in v5 (owner decision 2026-09-21, recorded in that repo's ROADMAP).
- [ ] **Upstream asks (go-cqrs-lite) — (3)+(4) RECORDED 2026-09-22 (D1) in that repo's TODO_LIST** ("Upstream asks from cqrs-htmx, harvested 2026-09-22"), together with a third ask: `System.Explain` per-query Volume/placement display (verified absent empirically).** (1) ~~Decouple `stack/v4`'s root package from `metaengine/v4`~~ REDECIDED 2026-09-21 (user decision, design-tree interview): **remove `stack/v4` entirely in go-cqrs-lite v5** instead of decoupling it — metaengine + `system/` is the intended direction, the coupling is direction-aligned, and cqrs-htmx's V007 migration deletes the `stack.*` usage anyway (making the metaengine require intentional). Recorded in go-cqrs-lite's ROADMAP (verified surviving 2026-09-22). ((2) RESOLVED: `cqrs-upgrade --workspace` shipped upstream and was used for the 2026-09-20 train-lag sweep.)
- [~] **Adopt appkit as the `setup` server layer (verdict consolidated in ADR-0052, 2026-09-22; Option A confirmed by spike, (b)–(f) assessed 2026-09-20 — all defer with rationale recorded in the ADR).** `RunWithAppkit` is stable since 2026-09-07 (go-appkit v0.5.x, no replace; SSE-flush/readiness/parity tests race-green). Remaining movement: only the v5-window revisit condition in ADR-0052 (health-dedup guidance + chain layering settled); nothing actionable before then.
- [ ] **Add cqrs-lint strict CI gate to GitHub Actions (same work as the P2 "wire remaining check-* apps" item — merged in the 2026-09-20 Pareto plan).** Run cqrs-lint `--strict` in CI to catch catalog/validation findings early. Blocked: cqrs-lint is a Nix-only binary; needs a Go-installable distribution or a Nix CI runner. The flake.nix `check-cqrs-lint` app exists for local use — state re-verified green 2026-09-22 (round 8; note: run it ONLY via the flake app — manual workspace-mode invocations surface phantom E014-class findings).
- [ ] **Hardware watch: `/mnt/buildcache` has a failure history.** The mount died 2026-08-16→2026-08-22 (every gate failing uniformly with "failed to initialize build cache") and was repaired (sdb1, 220G). **2026-09-22 (round 8): the disk is 100% FULL (1.8G free of 220G — dominated by rust/ 155G + sccache/ 20G, NOT the Go caches at ~12G total); it breaks every gate that resolves GOCACHE/GOLANGCI_LINT_CACHE there (the 2048MB low-disk guard fail-fasts correctly) and it made a templ-components buildflow pre-commit fail with "no space left on device".** **Resolved same day (~13:00):** reclaimed to 164–169G free by a concurrent session — but the fill-drain cycle recurs, so `df -h` first stays the rule (AGENTS gotcha 12 has the full note). Workaround used: /tmp fallback caches (29G free tmpfs; `GOCACHE=/tmp/go-build-cache GOMODCACHE=/tmp/go-mod-cache GOLANGCI_LINT_CACHE=/tmp/golangci-cache`) for session Go work. Needs a human decision: free space on the mount (rust/sccache are the reclaim candidates — NOT this repo's to delete unilaterally) or accept /tmp fallbacks. Also: the VCS-cache origin-loss class is now GATED (`scripts/check-vcs-cache.sh`, all 33 entries healthy). If the mount dies again: repoint caches to `/tmp` and check `df -h /mnt/buildcache` first when gates fail en masse.
- [ ] **Remove `ProjectionLayer` in the v5 removal bundle (all prep DONE 2026-09-15).** `// Deprecated:` marker shipped (systemadapter/projections.go:99); migration guide `docs/guides/declarative-projections.md` (API mapping, drain semantics, checkpoint-store caveat); `examples/system-demo` migrated off `NewProjectionLayer` (now uses `sys.Start` + declarative queries); SQLite-engine declarative tests (`TestDeclarative_SQLite_UserLifecycle`, `TestDeclarative_SQLite_AuthzPolicies`) + negative-path tests (`TestDeclarative_MissingLookups`) landed; adminui/dashboardui verified ProjectionLayer-free; test migration (plan step 5.3) is covered by the declarative suite + `TestDeclarative_EquivalenceWithProjectionLayer` (the remaining ProjectionLayer tests legitimately test the deprecated layer itself). The former "one honest gap" is RESOLVED 2026-09-20: accepted-limitation note recorded in `docs/guides/v5-removal-inventory.md` — the declarative path has no custom checkpoint/DLQ injection (`system.New` uses an internal in-memory checkpoint store), so consumers needing durable checkpoints or dead letters stay on `NewProjectionLayer` until an upstream `system.New` option; if none lands before v5, removal proceeds with the limitation documented.
- [ ] **Decide `examples/datastar-demo` rebrand-or-remove** (modularization assessment 2026-07-19 item 5). Owner call. **Evidence (2026-09-22, round 8): the demo is HEALTHY and accurately named — hermetic build + tests green on the published datastar v4.12.0 module, README documents 9 endpoints including replay, no deprecated API usage. Recommendation: KEEP AS-IS (no rebrand — the name says exactly what it is; no removal — it is the only Datastar-native example and ADR-0050's route-split story points consumers at it).** A stray `go.mod.*.tmp` artifact was cleaned. Decision remains the owner's. (`setup.NewFromSystem()` bridge: routed to ROADMAP Open Question 12 — build-or-reject decision lives there.)
- [ ] **Consumer-eye verification for templ-components v1.19.1.** Throwaway module outside the workspace `go get`s the published v1.19.1 tags and compiles a minimal consumer; pkg.go.dev render check (the v4.12.0 precedent: round-8 (a)7). Proxy propagation is currently assumed, not proven, from the consumer side. (Source: adoption-session report §e4, §f8.)
- [ ] **Exact-class-set CSS bundle drift gate.** The bundle builders canary utility FAMILIES only; a sorted-selector-set diff (committed bundle vs fresh build, exit 1 on delta) mechanizes the manual formatting-only proof the v1.19.1 adoption needed. Ship atomic per the gate checklist (checker + fixture self-test + flake app + check-modules stage + CI + README). (Source: adoption-session report §e3, §f13.)
- [ ] **FEATURES.md rows for the round-8 gates** — VCS-cache health, dep-budgets self-test, pre-push CI-parity hook (round-8 §f9; the honest-inventory sweep never added them).
- [ ] **dashboardui `templ.WithChildren` spike in the hybrid path.** Round-8 (b)6's upstream discovery (`templ.WithChildren(ctx, child)` populates children slots in strings.Builder rendering) makes `display.Grid`/`htmx.PolledRegion` usable there; spike it for the hand-rolled projection-health polling region and survey adminui/loginpage for the same workaround class. (Sources: round-8 §b6; release-train §f14; adoption §f17.)
- [ ] **ListNote render bench** in dashboardui `render_bench_test.go` (N16 pattern: bench per adopted family). (Source: adoption-session report §c6, §f12.)
- [ ] **Post-adoption exclusion-claims sweep.** Re-verify every "deliberate exclusions" claim in the adminui/loginpage/dashboardui READMEs against templ-components v1.19.1 — exclusion reasons rot as the library grows (proven twice: Grid/PolledRegion, ListNote count variant). (Source: adoption-session report §f26.)
- [ ] **loginpage adoption pass.** `recipes.AuthLayout`, `forms.Input`/`forms.Form`, `feedback.Alert` (AGENTS templ-components section names these as the remaining opportunities; loginpage is the only UI module still hand-rolling). (Source: adoption-session report §f27.)
- [ ] **Dark-mode surface pin re-check post-bump.** If the family ever remaps gray-800/900, consumer `@theme` blocks must re-pin literal darks under `prefers-color-scheme: dark` (AGENTS templ-components gotcha); verify adminui + dashboardui pins still correct after v1.19.1. (Source: adoption-session report §f28.)
- [ ] **Screen-reader check of the DLQ count note** (`role="status"` announce-on-load semantics — confirm the aria-label + live-region behavior matches a11y expectations). (Source: adoption-session report §f33.)
- [ ] **CopyButton contrast visual check in adminui** — tables that re-color ancestors were the original bug class v1.19.1's `text-gray-700 dark:text-gray-200` span fix targets; check the actual fleet scenario once. (Sources: adoption-session report §f14; release-train §f15.)
- [ ] **Count-notice consistency sweep.** Should dashboardui's audit/commands/queries list pages carry count notices too (same count-info shape), and are there other repo spots with count-only semantics? One sweep. (Sources: adoption-session report §f15, §f40.)
- [ ] **Gate-integrity spot checks post-bump.** (a) verify-tag fixture go.mods still exempt from the drift gate (adoption §f46); (b) e2e webServers `GOWORK` pin unchanged (adoption §f47); (c) pre-push hook behavior when BOTH repos have pending pushes (adoption §f48); (d) `check-vcs-cache` CI job placement double-check (round-8 §f44); (e) `go.work.sum` absorbed the v1.19.1 hashes cleanly — builds prove it works, a tidy pass costs nothing (release-train §f19).
- [ ] **Runbook: recovering from `fatal: cannot lock ref 'HEAD'` daemon races** (check `git log` first, re-stage, retry once or accept daemon pickup). Hit live 2026-09-22; the next session will hit it too. (Source: adoption-session report §f30.)
- [ ] **Concurrency-safety helper scripts.** `scripts/lib/preflight-tree-check.sh` (mechanical gotcha-4 step: `git log --oneline -1` + `git status --short` immediately before ANY batch-mutating loop, abort-on-surprise) and `scripts/lib/wait-tree-quiet.sh` (clean tree + N-second no-new-commit wait before `git town continue`/push retries). (Sources: release-train report §e1, §e4, §f8–9.)
- [ ] **agents-notes narrative: 2026-09-22 concurrent-session incidents.** The near-double-bump (verification-not-duplication save), the false "gutted CSS" alarm (mvdan/sh `<()` false-EQUAL + a story outrunning measurement), and the `git town continue` race — dated history belongs in `docs/agents-notes.md`; the distilled shell trap is already in AGENTS gotcha 3. (Source: release-train report §c6, §f7.)
- [ ] **Release-train gate UX: print the fix recipe.** On train-lag failure, output the exact per-module `go get` commands (copy-pasteable family alignment) next to the existing hint. (Source: release-train report §f20.)
- [ ] **Small hardening chores (post-round-8 sweep).** (a) wire `check-vcs-cache.sh` into `prewarm-gocache.sh` as well (round-8 §f35); (b) T9-class env-coupling audit: pin scratch caches in the `test-verify-tag` + `test-check-docs-*` fixture self-tests so machine disk state can never flip them (round-8 §f36); (c) README contributor note about the pre-push gate — what fires, the `--no-verify` escape, offline behavior (round-8 §f42); (d) archive-or-finish the stray `docs/status/2026-09-21_18-20_round6-*.md.new` leftover (release-train §f10).

---

_For completed work, see [CHANGELOG.md](CHANGELOG.md) and [git log](https://github.com/larsartmann/cqrs-htmx/commits/master). For long-term vision, v5 plans, and rejected ideas, see [ROADMAP.md](ROADMAP.md)._
