# Session Status — Pareto Plan Execution: Done, Half-Done, Gaps, Own Failures, Next 50

**Date:** 2026-08-30 21:35 CEST · **HEAD:** `ee4fbb7c` · **Origin delta:** 11 commits AHEAD (unpushed) · **Tree:** clean
**Scope:** execution of `docs/planning/2026-08-30_11-28_pareto-plan-unbreakable-releases-and-green-family-train.md`
(22 medium / 82 fine tasks) minus the plan's §5 user-gated items, which were PREPARED, not executed.

## Commit inventory (this session, oldest → newest)

| #  | Commit                                                     | Cluster                                                                                                                                                              |
| -- | ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | `9e46bd11`                                                 | M1 verify-tag hardening + 12-case fixture self-test + runbook §6                                                                                                     |
| 2  | `7a8f9f8e`                                                 | M2 dead-hooks discovery + tracked template + installer + self-test + testdata exclusion in 3 gates                                                                   |
| 3  | `7b8b5c07`                                                 | M3 retraction publication recipe (runbook §6b)                                                                                                                       |
| 4  | `2be49b85`                                                 | M4 LICENSE: 2 proprietary scaffolds → MIT + 9 missing licenses added                                                                                                 |
| 5  | `0466354a`                                                 | M5 flake flag passthrough for check-release-train                                                                                                                    |
| 6  | `0222011b`                                                 | M12/M13 + WithSSEFilter + decision one-pager + markdownlint config + go-cache-env hardening + hook sourcing + attic move + docs truth sweep (lumped cluster — see e) |
| 7  | `1943f67c`                                                 | styles.css: verified 166/166 class parity, minified artifact canonical (closes the hand-revert gotcha)                                                               |
| 8  | `ea0b4bb0`                                                 | M14/M15 contract suites + **the setup cookie split-brain fix**                                                                                                       |
| 9  | `78954ec0`                                                 | M8/M9 CI steps + M11 e2e preflight + M22 six decision docs + session report + CHANGELOG/TODO                                                                         |
| 10 | `ee4fbb7c`                                                 | lint debt from my own new code (cyclop/exhaustruct/wrapcheck/errname/bodyclose)                                                                                      |
| 11 | `9e46bd11`..`ee4fbb7c` — **11 commits total, none pushed** |                                                                                                                                                                      |

---

## a) FULLY DONE (executed, verified, committed)

| #  | Item                                                                                                                                                                                                                                                                                                    | Proof                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | **M1 verify-tag hardening** — refuses ANY local-path replace (family/sibling/absolute + replace-block bodies), parses single-line AND block requires (the old guards silently skipped `require x v` lines — a real hole), `--dry-run` now runs ALL guards, `--allow-replace-exempt` documented override | `test-verify-tag.sh` 12/12; fixture corpus includes the REAL poisoned `setup/v4.8.1` go.mod; real-repo smoke: health dry-run passes, systemadapter dry-run correctly refused                                                                                                                                                                                                                                                                                                             |
| 2  | **M2 dead-hooks discovery + bootstrap** — global `core.hooksPath=.githooks` but `.githooks/` DID NOT EXIST: the large-file guard, release-train gate, and GOCACHE prewarm were **silently dead for every commit**; `.git/hooks/pre-commit` was dead code                                                | template tracked at `scripts/hooks/pre-commit.template`; installer resolves repo>global>`.git/hooks`, `--force`/`--verify`; `test-install-git-hooks.sh` 10/10 incl. behavioral proof (scratch commit passes gates; staged 2 MB binary BLOCKED); gates re-verified live on real commits since                                                                                                                                                                                             |
| 3  | **M3 retraction prep**                                                                                                                                                                                                                                                                                  | runbook §6b: complete setup v4.8.4 recipe (gates, verify-tag lines, paste-ready CHANGELOG entry, proxy/pkg.go.dev checks); dry-run facts: retract parses, hermetic build green, `@latest`=v4.8.3 (not yet consumer-visible — correct)                                                                                                                                                                                                                                                    |
| 4  | **M4 LICENSE**                                                                                                                                                                                                                                                                                          | usermgmt + dashboardui shipped PROPRIETARY scaffolds (dashboardui: "Copyright Unknown Author", `unknown@example.com`) against MIT root/README/siblings; pkg.go.dev showed "License: UNKNOWN" AND suppressed documentation. Fixed to MIT + 9 published modules had NO license (adminui, loginpage, setup, systemadapter, health, auditlog, totp, webauthn, oauth2) — all now MIT. Renders per-module at each next tag                                                                     |
| 5  | **M5 flake passthrough**                                                                                                                                                                                                                                                                                | `nix run .#check-release-train -- --json/--strict-lag/--refresh-cache` verified working (was silently dropping all flags); `--json` shape + description updated                                                                                                                                                                                                                                                                                                                          |
| 6  | **M6 verification re-run at final HEAD**                                                                                                                                                                                                                                                                | test-flake 3/3 ✅ · test-fuzz ✅ · check-cqrs-lint ✅ (all modules) · check-modules `--report` 8/8 ✅ · runbook §7 + TODO_LIST header record it                                                                                                                                                                                                                                                                                                                                          |
| 7  | **M7 integration_test**                                                                                                                                                                                                                                                                                 | fullstack suite green hermetically (GOWORK=off) post-sweep — the "integration unproven" gap closed                                                                                                                                                                                                                                                                                                                                                                                       |
| 8  | **M8/M9 CI wiring**                                                                                                                                                                                                                                                                                     | `checks` job gained: shellcheck (docker, all scripts/*.sh), actionlint, verify-tag fixture self-test, install-git-hooks self-test — actionlint exits 0 locally; steps mirror the exact local commands                                                                                                                                                                                                                                                                                    |
| 9  | **M11 e2e preflight**                                                                                                                                                                                                                                                                                   | browsers-path writability fallback (the dead /mnt/buildcache ALSO poisoned ambient `PLAYWRIGHT_BROWSERS_PATH` — 3 tests failed on missing ffmpeg until fixed) + chromium/ffmpeg auto-provision; **e2e 4/4 green**                                                                                                                                                                                                                                                                        |
| 10 | **M12 /sse one-pager**                                                                                                                                                                                                                                                                                  | `docs/planning/2026-08-30_sse-endpoint-shape-decision.md`: options A–D table, costs/risks, recommendation (A now, B escape hatch, C on demand), answer template; linked from TODO_LIST + SSE guide                                                                                                                                                                                                                                                                                       |
| 11 | **M13 WithSSEFilter shipped**                                                                                                                                                                                                                                                                           | live via `SubscribeFilter`, replay fail-closed via `filteredEventStore` (any EventStore works; backfill can NEVER leak excluded events); 2 dedicated tests; SSE guide section; does not pre-empt the M12 decision                                                                                                                                                                                                                                                                        |
| 12 | **M14 store-hatch contracts**                                                                                                                                                                                                                                                                           | counting-store proofs: WebAuthn ceremony sessions, verification tokens (issued token consumable ONLY from the supplied store), pending-TOTP (park + consume via supplied store) — a silently-ignored custom store would strand multi-instance deployments                                                                                                                                                                                                                                |
| 13 | **M15 bundle SQL restart test**                                                                                                                                                                                                                                                                         | instance #1 registers over HTTP; instance #2 (same sqlite) must: authenticate the #1-issued session via the supplied store, 409 the duplicate email (hydration proof), /health 200 — and it CAUGHT A REAL BUG (see d-2)                                                                                                                                                                                                                                                                  |
| 14 | **M16 v5 removal inventory**                                                                                                                                                                                                                                                                            | `docs/guides/v5-removal-inventory.md`: 5 removal classes, 34 files swept, per-item criteria + sweep protocol; linked from ADR-0047                                                                                                                                                                                                                                                                                                                                                       |
| 15 | **M17 release playbook**                                                                                                                                                                                                                                                                                | `docs/guides/release-playbook.md` (3 rules, pre-tag checklist incl. the manual `git show <dep-tag>` content check, train order, poison ladder, post-train hygiene); cross-linked from runbook                                                                                                                                                                                                                                                                                            |
| 16 | **M18 docs truth sweep**                                                                                                                                                                                                                                                                                | AGENTS: templ-components family v1.11.0 (five modules), go-cqrs-lite v4.9-era list (verified vs go.mod), Go 1.26.7 row (was "1.26.6 / 1.26.5 note" — stale), record/v4 V006 comment fixed; bench README current-pin section (medians + load1 2.20); runbook §0 marked HISTORICAL; AGENTS guides list 20→22                                                                                                                                                                               |
| 17 | **M19 tag cache v2**                                                                                                                                                                                                                                                                                    | `--json` gained `cache_age_seconds` (verified: 649); stale-cache hint >¾ TTL; `--refresh-cache` confirmation; shellcheck clean                                                                                                                                                                                                                                                                                                                                                           |
| 18 | **M20 small closes**                                                                                                                                                                                                                                                                                    | F20.1 smoke = proven by live events (the reactivated train gate blocked a real commit — see d-4) · F20.2 batch-release.sh → `attic/` + README · F20.3 GONOSUMCHECK verdict: non-official legacy var, zero active need (all repos public; sum.golang.org proven by every hermetic build) · F20.4 coverage-gate 15/15 · F20.5 docs-freshness green                                                                                                                                         |
| 19 | **M22 decision docs**                                                                                                                                                                                                                                                                                   | 6 prepared docs + index (`docs/planning/2026-08-30_gated-work-index.md`): v4-branch purge (rec: DELETE the branch), setup-demo blob purge (**rec: DO NOT rewrite** — proxy-cached tags make it consumer-hostile), buildcache hardware (rec: repoint caches to persistent NVMe), cqrs-lint Go-distribution draft, upstream asks (projectionadapter v4.5.0 = THE blocker), appkit fold-in **re-validated UNBLOCKED** (go-appkit wave verified pushed; all 6 checklist premises re-checked) |
| 20 | **5 real defects found & fixed** (details in d)                                                                                                                                                                                                                                                         | dead hooks; setup cookie split-brain; verify-tag single-line-require hole; testdata scanning noise in 3 gates; dead-mount poisoning of GOLANGCI_LINT_CACHE + PLAYWRIGHT_BROWSERS_PATH                                                                                                                                                                                                                                                                                                    |

**Final gate matrix (re-run at HEAD `ee4fbb7c`):** build ✅ · test (all suites) ✅ · lint 0 issues / 15 modules ✅ · flake-check ✅ · coverage-gate 15/15 ✅ · check-modules --report 8/8 ✅ · release-train (0 unpublished / 1 exempt / 0 lag) ✅ · strict drift ✅ · replace-directives ✅ · docs-links ✅ · docs-freshness ✅ · e2e 4/4 ✅ · test-flake 3× ✅ · test-fuzz ✅ · cqrs-lint ✅ · verify-tag self-test 12/12 ✅ · hook self-test 10/10 ✅ · integration_test ✅.

## b) PARTIALLY DONE

1. **M13 productionization** — mechanism shipped in `transport`; the setup-side wiring (a scoped `SSEPath` option) deliberately NOT added: it would pre-empt the user's /sse decision. One small option away once decided.
2. **M8/M9 CI steps** — added and locally linted (actionlint 0, shellcheck 0), but they have NEVER RUN on a real Actions runner (the branch is unpushed). First push will be their first execution; the docker-pull steps (koalaman/shellcheck, rhysd/actionlint) are untested for runner network/docker availability quirks.
3. **M18 docs truth** — the flagged items are fixed, but the sweep was targeted, not exhaustive: TODO_LIST header still carries "Coverage verified 2026-08-29" numbers (now 93.2/82.0/85.9 etc. per today's gate run — tiny drift, harmless), and other AGENTS claims were only spot-checked.
4. **M22** — all six decision docs exist with recommendations, but zero decisions made and zero actions executed (by design: force-push/external class).
5. **Cookie fix blast radius** — the setup fix changes behavior for every default-composition consumer (from broken to working). setup-demo tests + e2e + full setup/usermgmt suites verified green, but no REAL consumer deployment has been exercised against it yet.
6. **styles.css canonicalization** — parity proven on class SELECTORS (166/166); the custom `@property`/keyframes blocks were spot-checked in the minified output, not diff-exhaustively. Risk ≈ zero but not literally proven byte-for-byte on non-selector rules.
7. **Push state** — 11 commits sit unpushed on master-local. Everything is verified locally; origin is 11 behind. (Listed here because "done" isn't "done" until CI sees it.)

## c) NOT STARTED (all gated, per plan §5 — none forgotten by accident)

1. **M10** — CI blocking flip + `--strict-lag 0` + drift leg: calendar-gated (green advisory week ends ~2026-09-06). Flip procedure already documented.
2. **M21** — the train (upstream tag → strip → systemadapter tag → family cut): externally blocked on go-cqrs-lite `metaengine/projectionadapter/v4 v4.5.0` (verified: max is v4.4.1, sibling clean+synced) + unanswered g2 (version number; recommendation on record: v4.8.0).
3. **M3-execution** — actually cutting setup v4.8.4: gated on g1 (retraction timing).
4. **M13-execution decision** — the /sse endpoint-shape pick (A/B/C/D): user's call; one-pager waiting.
5. **M22 executions** — the actual force-pushes / hardware purchase / new-artifact publication / upstream issue filing / appkit fold-in: all prepared, none executed.
6. Nothing else from the plan's 50-item source queue remains untouched — every §f item maps to an executed task or a gate note above.

## d) TOTALLY FUCKED UP (own failures this session — all recovered, all instructive)

1. **Sloppy multi-edit anchors broke files twice**: a stray newline-eating edit joined `fi` + `if cmp` in the installer (syntax error) and `do` + `[ -n "$dep" ]` in verify-tag (valid but ugly). Root cause: constructing old_string/new_string pairs with trailing-newline mismatches instead of re-reading. Cost: ~3 debug cycles each.
2. **My scratch test wrote into the REAL repo**: `git -C "$S" rev-parse --git-path hooks` returns a RELATIVE path; I used it from the session cwd, creating a stray baseline `.githooks/pre-commit` in the live repo. Irony: the bug this test was written for (hook-path confusion) bit the test itself. Fixed (absolute paths) + recovered (template deployed over the stray).
3. **Bad sed for shellcheck directives**: em-dash + prose after `# shellcheck disable=` broke directive parsing (SC1125), then the sed "fix" corrupted a verify-tag line into an unparsable double-command. Took three attempts to land a clean directive-only form. Lesson: directives are exact-syntax; edit them by hand, never by pattern.
4. **My own M1 fixtures poisoned the gates**: `check-release-train`/`version-drift`/`replace-directives` scan `find . -name go.mod` — the new `scripts/testdata/verify-tag/*/go.mod` fixtures registered as 25 phantom train-lag entries + 1 phantom UNPUBLISHED, which then **blocked a real commit** via the reactivated pre-commit train gate. Fixed by excluding `*/testdata/*` in all three scripts (verified 664 requires / 0 findings). Embarrassing and instructive: I shipped fixtures hours before my own gate choked on them — only the reactivated hook caught it.
5. **Forgot `GOLANGCI_LINT_CACHE` in one commit command's env exports** → buildflow's golangci steps sharded onto the dead `/mnt/buildcache` → 13-step pipeline failure. The ambient parent-shell env still carries the dead-mount paths; the /tmp layout must be exported EXPLICITLY every time. Root-caused into a structural fix (hook now sources `go-cache-env.sh`, which validates GOLANGCI_LINT_CACHE too) instead of leaving it as personal discipline.
6. **`git add -A` absorbed the styles.css husk** (1517-line minified regeneration) into a commit; then the `--amend` re-triggered the hook which REGENERATED and re-staged it — the exact self-defeating loop the gotcha warns about. Escaped via the documented `--no-verify` amend fallback after proving 166/166 class parity and promoting the minified artifact to canonical (root-fixed the gotcha properly, with AGENTS update).
7. **Committed transport code that passed tests but NOT the lint gate**: the pre-commit golangci (staged-only mode) passed `0222011b`, yet the full `nix run .#lint` found 4 issues (cyclop 14>12, exhaustruct, 2 wrapcheck). Two different lint scopes giving different verdicts = the hook gives false confidence on gate strength. Fixed in `ee4fbb7c`; the discrepancy itself is logged as improvement e-2.
8. **Parallel heavy runs interfered**: the coverage-gate failed (setup coverage step) while my foreground setup suite ran concurrently; re-run on an idle machine → 15/15 green. Wasted a cycle; the gate is effectively not concurrency-safe (logged in e-6).
9. **Four iterations on the bundle-restart test** (unused ctx import, wrong JSON field path, wrong cookie NAME, wrong response shape, wrong middleware chain) before green — each trivial, but the sequence shows I wrote the test against ASSUMED shapes instead of reading handleMe/Mount/register-response first.

## e) WHAT WE SHOULD IMPROVE

1. **Make the pre-commit lint gate as strong as the full sweep** — staged-only golangci passed code the full lint rejects (cyclop/exhaustruct class). Align buildflow's staged lint scope with `nix run .#lint`, or add a per-module full-lint step to the pre-commit chain.
2. **Push cadence** — 11 verified commits, zero on origin. Every hour unpushed is risk (disk, daemon conflicts, no CI signal). The M8/M9 CI steps cannot earn trust until they run.
3. **Stop using `git add -A`** in this repo — the styles.css husk absorption is the second time aggregator staging ate an artifact regeneration. Stage explicit paths; keep `-A` for nothing.
4. **Tests must never write outside their sandbox** — enforce absolute-path discipline in scratch-based scripts (the d-2 bug); add a self-check asserting the scratch dir is the cwd before any write.
5. **Commit clusters honestly sized** — `0222011b` lumped ~7 plan tasks under a transport message; the CHANGELOG had to carry the real inventory. One logical change per commit, or at least a message that enumerates the lump.
6. **Serialize gate runs** — coverage-gate (and possibly check-modules) are not safe under concurrent heavy suites on this machine; document "one gate at a time" or add a lockfile.
7. **Read response shapes before writing tests** — the M15 iteration count was a planning failure, not a tooling one: curl the endpoint or read the handler before drafting assertions.
8. **Fix the ambient env at the shell level, not per-command** — the parent shell still exports dead `/mnt/buildcache` paths for GOCACHE/GOMODCACHE/GOLANGCI_LINT_CACHE/PLAYWRIGHT_BROWSERS_PATH. The repo now defends itself (go-cache-env + flake fallbacks), but the machine-level env should be corrected once (hardware decision doc option B covers this).
9. **bump the memory record** — AGENTS Quick Reference "Go caches" row and hardware doc both name the /tmp layout; if hardware option B is approved, update all three places in one change.
10. **e2e auto-install latency** — `bun x playwright install` runs on every invocation when no Nix chromium is present; add a cheap marker/probe so the common path stays fast.

## f) NEXT — up to 50 things to get done (Pareto-ordered, gated ones marked 🔒)

**Now (unblocked, in-repo)**

1. 🔒 Push the 11 commits (needs your go-ahead) — CI then runs the new shellcheck/actionlint/verify-tag/hook steps for the first time (M8/M9 close).
2. 🔒 Answer the /sse decision (A–D) in `docs/planning/2026-08-30_sse-endpoint-shape-decision.md` — unblocks M13-exec if B/C.
3. 🔒 Answer g1 — then run runbook §6b (setup v4.8.4 retraction) or fold it into the next train.
4. 🔒 Answer g2 (systemadapter version — recommendation: v4.8.0) + authorize the go-cqrs-lite `projectionadapter v4.5.0` tag (upstream draft ready) — unblocks ALL of M21.
5. Wire a setup-side scoped-SSE option (`SSEFilter` predicate passthrough to `transport.WithSSEFilter`) — becomes a 20-line task post-decision.
6. 🔒 M10 flip (~2026-09-06): remove `|| true`, add `--strict-lag 0`, flip drift leg, update TODO_LIST/AGENTS CI posture.
7. Per-module full-lint step in the pre-commit chain (close the staged-vs-full lint gap, e-1).
8. Add a `--message`/cluster-enforcement hint to commit tooling OR a pre-commit check that staged-set ≠ message claims (lightweight).
9. Coverage-gate concurrency lock (flock) so parallel suites can't corrupt a gate verdict.
10. Hardware decision execution — if option B approved: repoint caches to persistent NVMe in one change (go-cache-env fallback order + AGENTS row + devShell env + warm-up build).
11. appkit fold-in execution (ADR-001) — re-validated unblocked; one-change plan written (setup `RunWithAppkit` merge + chain dedupe + health-target doc + bench).
12. File the upstream projectionadapter ask (draft ready, 🔒 your send).
13. cqrs-lint Go-distribution: extract analyzer → `cmd/cqrs-lint`, tag v4.6.0-parity, then flip `check-cqrs-lint` from blocked → enforced in CI.
14. LICENSE visibility pass: after the next family train, re-fetch pkg.go.dev for the 11 touched modules and confirm "MIT" + un-suppressed docs (close M4 fully).
15. Add LICENSE-presence check to `check-modules` (any published module without a LICENSE file = hard fail) so M4 can never regress.
16. Extend verify-tag with an optional `--verify-dep-content <dep>:<tag>:<path>:<symbol>` helper that automates the manual stale-tag `git show` check (the one guard it admits it cannot do).
17. Add the real systemadapter poisoned-tree fixture to the corpus once its tag exists (today it's refused by the live-repo smoke, not a committed fixture).
18. Diff-exhaustively prove styles.css non-selector parity (custom @layer/@property blocks) to fully retire b-6.
19. setup-demo: wire its e2e into a flake app (it currently only runs via manual per-module `go test`, outside `.#test`'s examples exclusion).
20. Investigate why the pre-commit buildflow golangci (staged mode) skips cyclop/exhaustruct-class findings — root-cause the scope difference, not just the symptom.
21. Add `cache_age_seconds` + stale-hint documentation to the runbook §6 tag-protocol section (feature shipped, docs one line behind).
22. Tag-cache: persist per-repo fetch timestamps so `--refresh-cache` can print per-repo ages, and consider halving TTL for release-candidate runs.
23. e2e: pre-provision browsers in the devShell hook (kill the auto-install latency path entirely).
24. M15 extension: assert the checkpoint row itself survived (read `checkpoints` table directly) — today the 409+health prove hydration indirectly.
25. M14 extension: TOTP contract test with a REAL valid code (compute via the test provider) so `VerifyTOTPSetup` succeeds end-to-end, not just consumes.
26. Contract test for the AuditLog hatch (`ServiceConfig.AuditLog`) — the one ServiceConfig store not yet covered by a counting proof.
27. Contract test for `LockoutStore` cross-instance semantics (two services, one store: lockout recorded on #1 visible to #2).
28. usermgmt SQL session store for sqlite (parity with the MySQL `CreateSessionStore`) — would let M15 prove session survival with a REAL SQL store instead of a shared in-memory stand-in.
29. Make `SessionMiddleware` failure mode loud: debug-level "session authentication failed" is invisible in default setups (it hid the cookie bug for releases) — add an opt-in startup self-check that round-trips a session through handler+middleware cookie names.
30. Add a setup `New` smoke option (`Config.SelfCheck`) performing the cookie round-trip at startup in dev mode.
31. Sweep `//nolint` directives added this session into the stale-suppression detector runs (cqrs-lint 4.6.0 feature) so they age out if the underlying rule stops firing.
32. Retire `backup/pre-blob-purge` + `refs/original/*` (2026-08-14 filter-branch leftovers) once the blob-purge decision is final.
33. Delete the dead `.git/hooks/pre-commit` copy (superseded by `.githooks/`) — or make the installer also sync it to avoid future confusion.
34. AGENTS: add a one-line "shell scripts are treefmt-owned; buildflow excludes them" note next to the formatter-division gotcha (the exclusion lives in `.buildflow.yml`, easy to miss).
35. Run `nix run .#check-docs-freshness` on the three new planning docs after decisions land (they self-declare status fields the checker may not know).
36. Normalize the six M22 docs into the docs-freshness parser's known status vocabulary (they use "Status: PREPARED", the checker understands DONE/open conventions only).
37. CI: add the `check-release-train --json` output as a job summary artifact (machine-readable history of the train state per run).
38. CI: cache shellcheck/actionlint docker pulls (or switch to pinned apt versions) to shave runner minutes.
39. Add `attic/` exclusions to the gate scripts' `find` (batch-release.sh moved there still matches `scripts/*.sh`-style scans in some tools).
40. Write the v4-to-v5 migration guide skeleton (`docs/migrations/v4-to-v5.md`) referenced by ADR-0047 but not yet existing — the inventory doc is its data source.
41. Record in CHANGELOG's next release block: LICENSE visibility per-module at next tags (consumer-facing improvement worth announcing).
42. Bench: after the appkit fold-in, re-pin the baseline (policy already says re-pin on bench-path changes) and update the current-pin section.
43. Consider `WithSSEFilter` convenience: a `FilterByStreamType(types ...string)` helper predicate (the 95% use case, avoids hand-rolled JSON-substring preds).
44. Sweep examples for `NewAuthHandler`-style default-cookie assumptions (pattern-check that no other module pairs handler+middleware with independently-defaulted names).
45. Update `docs/guides/fullstack-wiring.md` with the cookie-alignment gotcha (consumer-facing version of the setup fix).
46. TODO_LIST: harvest b-3's coverage-number drift in the header during the next routine docs pass.
47. Add the `--allow-replace-exempt` usage (if ever used) to the release-notes checklist row in runbook §6.
48. Keep a `git push` cadence rule: any session ending >3 commits ahead triggers an explicit push-or-why note in the report.
49. When projectionadapter v4.5.0 lands: strip the last 2 replaces (systemadapter + system-demo), run the full M21 script — the runbook is the checklist; systemadapter's first tag then makes release-train exemptions 1→0 and unblocks M10's `--strict-lag 0` permanently.
50. After the next family train: run the post-train HARVEST session (fold §f items into CHANGELOG, re-derive runbook §7 stage floor, re-verify pkg.go.dev tags + licenses).

## g) Questions only you can answer

1. **Push?** 11 verified commits (incl. the setup cookie fix and the CI steps) sit unpushed on master-local. Push now, or hold?
2. **/sse endpoint shape:** A (ship session-gated as-is), B (session-gate + scoped filter option), C (per-user/per-tenant streams), or D (drop the shared /sse)? One line answers it — the one-pager's recommendation is A.
3. **Retraction timing (g1):** cut patch-only `setup v4.8.4` now to publish the v4.8.1/v4.8.2 retraction, or fold it into the next family cut (M21)? Recipe is ready either way.

---

_Prepared per standing instruction: report only — no further actions taken after the commit of this file._
