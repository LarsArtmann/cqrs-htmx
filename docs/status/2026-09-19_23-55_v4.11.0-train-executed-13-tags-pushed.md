# Status Report: v4.11.0 Family Release Train — EXECUTED (13/13 tags pushed)

**Session:** 2026-09-19, ~17:24 → 23:5x CEST (continuation of the 16:42 + 18:03 sessions)
**Head at write time:** `87710c9b` (pushed) — plus an in-flight hygiene commit for this report
**Milestone:** the complete v4.11.0 coordinated family train is CUT, PUSHED, and PROXY-VISIBLE. All 13 tags on origin. `check-release-train`: **0 unpublished / 0 train lag / 1 replace-exempt** (the deliberate systemadapter exemption). Master CI run `35471359081` was still in progress at write time — see §(b).

---

## (a) FULLY DONE

| # | Work | Evidence |
|---|------|----------|
| 1 | **Lint blocker root-caused + fixed**: `exhaustruct_v5 v5.0.3` (bundled in golangci 2.13.2) panics (`makeslice: cap out of range`) on promoted-key struct literals — the Go 1.27 `modernize --fix` sweep (832bea8b) had rewritten 16 dashboardui library-prop literals into that form. All 16 reverted to nested `BaseProps` behind `//nolint:modernize`; dashboardui lint 0 issues, tests green (golden markup identical), committed `654075d5`. | Bisect proof: v4.10.1 green, e425d980 reports findings without panic, post-832bea8b panicked. |
| 2 | **71 internal requires bumped to v4.11.0 across 20 go.mods** (root→examples, incl. e2e/server). systemadapter stays at v4.9.0 (untagged by design; replace-exempt). `go mod edit` only — no tidy pre-tag. Post-condition greps: 71× v4.11.0, zero stale family versions, zero truncated paths (regex-corruption class from the earlier session did NOT recur). | Commit `60a6b177` (amended onto the daemon's heuristic snapshot). |
| 3 | **7 CHANGELOGs cut** for the train: root (`[v4.11.0] - 2026-09-19` + intra-window sub-train note + Go 1.27.1 floor + modernize/exhaustruct entries), usermgmt (audit-chain section; **MaxUsers drift fixed** — moved to its real `[v4.10.0] - 2026-09-10`), dashboardui, datastar, adminui (+ v4.8–v4.10 stub), loginpage (+ stub), identity-model (+ stub, honest "dep carrier" framing). | Same commit as (2). |
| 4 | **All 13 tags cut via verify-tag.sh and pushed**: identity-model → root → usermgmt → totp → webauthn → oauth2 → datastar → health → auditlog → adminui → loginpage → dashboardui → setup. Every cut ran `--dry-run` first; every push ls-remote-verified. | 13× "pushed and visible on origin". |
| 5 | **Family dev-replaces stripped** (all removal conditions met by the pushed tags): setup ×2 (required BEFORE its tag — verify-tag refuses local-path replaces), integration_test ×2, systemadapter ×2 (root/identity-model/usermgmt family; **kept** projectionadapter + metaengine sibling replaces — upstream-blocked). | In train commits + daemon snapshots `6bc303ef`/`42176acc`/`700780b6`. |
| 6 | **go.sum sync + hermetic verification for 24 modules**: `GOWORK=off go mod tidy` + build + vet all green (tags resolve from the proxy). | `/tmp/sync-verify.sh` run: `failed=0`. |
| 7 | **CI infrastructure hole root-caused and fixed**: `go.work` was **gitignored and untracked** — CI checkouts never had it. module-architecture's isolation/budget steps silently discovered ZERO modules (false "✓" in 0.04s) and the toolchain check hard-failed ("exit-2, never root-caused" since its first appearance — THIS was it). Every go.work-dependent CI gate (release-train blocking!, drift, isolation) has been silently dead on master. `go.work` is now tracked (load-bearing replace comments); go.work.sum stays untracked. Commit `ae5a567e`, pushed. | Run 35463720259 logs: `go: no go.work file found` in repo root; isolation step "✓ All modules" in 0.04s. |
| 8 | **mod-tidy failure fixed**: usermgmt/oauth2's one-line requires tidied to canonical block form (`go mod tidy -diff` clean). | Same commit. |
| 9 | **examples/basic cleanup**: the manual `core.ApplyOptions(cqrshtmx.CommandOptionsFromContext(...))` workaround dropped (automatic since root v4.11.0's structural-interface fix); NOTE comment rewritten to describe the automatic behavior. Workspace build+vet green. | Local verify; committed by daemon snapshot. |
| 10 | **Proxy spot-checks**: `go list -m -versions` shows v4.11.0 for root, setup, identity-model. | Proxy via GOTOOLCHAIN=go1.27.1. |
| 11 | **AGENTS.md hygiene**: Language row → Go 1.27.1; toolchain tug-of-war entry marked RESOLVED (analysis preserved); 5 new gotchas (exhaustruct_v5 promoted-key panic, shared-/tmp collision, `nix run <url>#app` cwd trap, go.work tracking, release-train-gate mid-train `--no-verify` mode). | In-flight hygiene commit. |
| 12 | **TODO_LIST sync**: toolchain policy decision (P2 ❓) removed — RESOLVED by the executed Option B; command-audit tail item trimmed to its true remainder (bench-spike re-run, requestContextEnricher upstreaming). | Same commit. |
| 13 | **Session recoveries held**: stale index.lock removed only after verifying zero git processes on this repo (0-byte, 10 min, held by a foreign project's process); no sibling WIP reverted (readiness.go `NamedCheck.Timeout` work absorbed into a daemon snapshot and explicitly attributed in commit messages). | Throughout. |

## (b) PARTIALLY DONE

1. **Master CI green on the train HEAD** — run `35471359081` (on ae5a567e) was in_progress at write time; `build` and `mod-tidy` jobs already showed failure marks mid-run. Needs: wait for completion, pull logs, fix. The module-architecture job is the critical acceptance: with go.work tracked, isolation now REALLY builds all modules on CI — first honest run ever, so new failures there are *newly visible*, not new damage. (mod-tidy risk: the job's hardcoded module list is stale — missing async-startup-demo, system-demo, setup-demo, middleware-showcase, samber-do-demo, e2e/server, catalog-demo is present; expanding it is queued, see §(f).)
2. **Status report** (this file) — written, not yet committed/pushed at write time.
3. **daemon-commit hygiene**: 9 of this train's commits carry heuristic daemon messages with real content (the daemon outran 3 deliberate commits). Content verified correct each time; history readability suffered — noted in §(e).
4. **check-modules full gate** (isolation+drift+replace+release-train bundle) ran as release-train standalone (0/0/1) but not the whole `nix run .#check-modules` bundle post-strip — queued after CI.

## (c) NOT STARTED (this session)

1. `bench-spike` idle re-run + 10%-gate decision (dispatch-audit chain ~275 ns/dispatch may trip it).
2. pkg.go.dev `/fetch/<module>@v4.11.0` warm-up ×13 + docs-render spot check.
3. Clean-dir `go get github.com/larsartmann/cqrs-htmx/v4@v4.11.0` + `setup/v4@v4.11.0` consumer smoke test.
4. Runbook §7 post-train hygiene entry for this train.
5. Upstream issue to exhaustruct_v5 (promoted-key panic, with the minimal repro from this session) — needs the verify-before-filing gate first.
6. `docs/benchmarks/setup-baseline.raw.txt` re-pin if bench-spike trips.
7. mod-tidy job's hardcoded module list expansion (missing 6 modules).
8. Release announcement/notes packaging from the new CHANGELOG sections.

## (d) TOTALLY FUCKED UP (honest list, all recovered or contained)

1. **The exhaustruct_v5 chase (~6 wasted lint cycles).** My first promoted-literal sweep grepped for the exact `ID: "", Class: "", Attrs: nil, AriaLabel: "", Nonce: ""` shape — and three sites carried VARIABLE values (`AriaLabel: ariaLabel`, `Attrs: attrs`) or multiline form, so they evaded every grep. I reverted 13 sites, linted, still panicked; reverted 2 more, still panicked; only the byte-level `diff -r` against the green e425d780 checkout exposed the last one. **Lesson applied and recorded:** sweep `--fix` damage by COMMIT DIFF, never by pattern grep.
2. **Instrumenting the module cache was a dead end** — I patched exhaustruct_v5's source in GOMODCACHE expecting golangci to pick it up; golangci is a prebuilt binary with analyzers compiled in. One wasted cycle; patch reverted cleanly.
3. **/tmp filename collision with the sibling session**: both sessions wrote `/tmp/lint-full.log`; I briefly analyzed go-cqrs-lite's 85-module lint output as if it were ours. Fixed with unique paths; gotcha recorded in AGENTS.md.
4. **I introduced a bogus placeholder replace** (`dashboardui/x-none`) into integration_test/go.mod during the strip (constructed an edit with a placeholder instead of a deletion) and had to remove it immediately. Caught by the same-commit grep; never committed.
5. **Daemon races shredded 3 deliberate commits + split stats.go out of the dashboardui fix** (heuristic commits `c89db8ec`, `eb60d4bd`, `6bc303ef`, `796fe792`, `377ace2e`, `42176acc`, `700780b6`, `e243a552`, `87710c9b` carry real content). I amended one daemon commit with an honest message; the rest were content-verified and left. The sibling's readiness WIP got absorbed into master — content preserved, attribution mixed (documented in the commit bodies).
6. **Two `--no-verify` commits were necessary** (bump + setup strip): the release-train pre-commit gate has no mid-train mode and fails by design on unpublished-next-version requires. Both carry full justification in-message; both content-verified independently first. Sanctioned by AGENTS.md, still worth automating away (§(f) #3).
7. **A stale index.lock was removed** after 10 minutes — verified 0-byte and held by a foreign project's process (dnsblockd session's git), not deleted blindly. Precedent-compliant but always worth re-stating.

## (e) WHAT WE SHOULD IMPROVE

1. **Sweep by diff, not by grep** — any "revert what --fix did" task starts at `git diff <before>..<after>`, full stop.
2. **Verify the runner actually RAN**: a 0.04s "✓ All modules" was a false green for months. Gates should assert work done (modules-checked > 0), not just exit 0 — same class as the gosec "Files: 0" lesson.
3. **The release-train pre-commit gate needs a `--allow-unpublished-next` env knob** so train-phase commits don't need `--no-verify`.
4. **golangci parallel-lock contention** under concurrent sibling sessions produced 2 hook failures on content that lints clean manually — consider per-repo golangci caches or a hook retry.
5. **The daemon will always outrun deliberate commits during long verify tails** — commit at phase boundaries remains the only defense; consider a daemon pause file (`/tmp/cqrs-htmx-daemon-pause`) if the script supports it.
6. **mod-tidy's hardcoded module list** duplicates go.work knowledge and already lags by 6 modules — switch it to a discovered list (with go.work now tracked, CI can do this reliably).
7. **`forEachGoModule`'s silent-empty fallback** should fail loudly when go.work is missing AND the invocation is CI.
8. **Verify-tag could gain a `--strip-replace-check-order` hint**: the setup tag needed its replaces stripped BEFORE the tag (verify-tag refuses replaces), contradicting the plan's "strip after all tags" ordering — the runbook should encode per-module ordering (replaces-carrying tagged modules strip first).

## (f) NEXT 50 (prioritized, actionable)

**P0 — finish this train (today):**
1. Wait for CI 35471359081; triage `build` + `mod-tidy` failures; fix; push; repeat to green.
2. Confirm module-architecture green (first REAL run) — any finding it now surfaces is newly-visible tech debt; triage individually.
3. Commit + push this report + the AGENTS.md/TODO_LIST hygiene edits.
4. Full `nix run .#check-modules` post-strip (isolation+drift+replace bundle).
5. Full battery one more time on the final HEAD: `.#build`, `.#test`, `.#lint`, `.#coverage-gate`, `.#check-cqrs-lint`, command bijection.
6. pkg.go.dev `/fetch/...` warm-up ×13 + license/docs render spot.
7. Clean-dir `go get` consumer smoke: root@v4.11.0 + setup/v4@v4.11.0 (+ `go build` the toy module).
8. Runbook §7 entry for the v4.11.0 train (what happened, timings, the go.work discovery).
9. bench-spike on idle machine; re-pin `docs/benchmarks/setup-baseline.raw.txt` in the same change if the 10% gate trips.
10. CHANGELOG root entry for the go.work tracking fix + module-architecture resurrection (consumer-visible CI posture change).

**P1 — high-value follow-ups (this week):**
11. Fix `forEachGoModule` silent-empty fallback (fail loudly in CI when go.work missing).
12. Add `--allow-unpublished-next` mode to the pre-commit release-train gate.
13. Expand mod-tidy job list to all 28 modules (or discover from tracked go.work).
14. File the exhaustruct_v5 promoted-key panic upstream (verify-before-filing gate; minimal repro = the buttons.go literal pair).
15. Move `requestContextEnricher` upstream to go-cqrs-lite event/ (TODO_LIST remainder).
16. Audit the other CI jobs for the same false-green class: assert real work counts (modules built, files scanned) in every gate.
17. Re-run `check-docs-freshness` and fix any stale claims the train created (setup replace mentions, dashboardui adoption table counts).
18. Update `docs/guides/release-playbook.md` with the setup-strip-first ordering + the go.work-now-tracked change.
19. Screenshot pass on adminui/dashboardui against v4.11.0 (train shipped UI-module bumps; visual regression net).
20. Verify `nix run .#e2e` still green post-train (Playwright, PLAYWRIGHT_BROWSERS_PATH fallback per AGENTS.md).

**P2 — debt & robustness (this month):**
21. golangci-lint parallel-lock: per-repo cache dirs or hook-level retry for sibling-session contention.
22. Daemon pause mechanism (pause file honored by the auto-commit daemon) to protect deliberate commits.
23. Deduplicate the replace-exemption logic drift risk: `lib/replace-exemption.sh` is single-sourced — add a test for the systemadapter exemption case.
24. systemadapter: track upstream projectionadapter v4.5.0 + metaengine Reset API; strip the 2 remaining replaces when tagged (removal conditions in go.mod comments).
25. go-datastar broadcast lockstep: the temporary family replaces are gone — verify nothing else pins the sibling.
26. Retire the deprecated `datastar.Broadcaster` facade early if zero external consumers (survey via pkg.go.dev reverse deps).
27. `check-dep-budgets.sh`: add module-count assertion (>0) to catch discovery failures.
28. `check-module-isolation.sh`: assert discovered-module count matches go.work use-count.
29. CI: cache Go builds between jobs (GOCACHE restore) to cut the 5-minute runs.
30. Templ-components v1.19 heads-up (Cards sharp by default) — pre-book the CSS rebuild + screenshot pass for the next train.
31. Audit remaining `*_reexport.go` deprecated layers (SSE re-exports, CSRF/ratelimit/server-timing) for v5 removal inventory accuracy.
32. v5-removal-inventory refresh: this train resolved several items; sweep the doc.
33. Add the `NamedCheck.Timeout` feature (sibling WIP now on master) to root CHANGELOG + README once its session completes it.
34. Integration test for the publish path: a CI job that runs `check-release-train --refresh-cache` against the just-pushed tags (catches phantom-tag class at the gate).
35. `verify-tag.sh`: add a `--from-plan <file>` batch mode that walks the dependency order (13 invocations → 1).

**P3 — quality & craft (backlog):**
36. Coverage gates re-baseline check under Go 1.27 (thresholds held this train; verify again after bench-spike).
37. `cqrs-lint` upgrade check (installed 4.8.1; the findings-gate triage listed ~11 findings pending from the version drift).
38. Sweep for other modules with local one-line `require` formatting drift (mod-tidy canonicity) — `go mod tidy -diff` across all 28.
39. Replace the ad-hoc `/tmp` scripts used this session with durable, commented `scripts/train-*.sh` helpers (bump, sync-verify) if the next train wants them.
40. Document the `/tmp` unique-path convention in AGENTS.md gotchas (done) AND in the sibling go-cqrs-lite repo (its session hit it too).
41. Consider committing `go.work.sum` under a CI-only generation step instead of leaving it fully untracked (removes the per-workspace regeneration cost for hermetic builds).
42. Add a `make`-free task runner entry (`nix run .#train-status`) that prints per-module tag-vs-require alignment in one table.
43. Split `CHANGELOG.md` root Unreleased folding rule into the docs-health skill (the intra-window sub-train note pattern is reusable).
44. Review whether `e2e/server` + `examples/*` should stay on the release-train lag=0 policy (they force 20 of the 71 bumps); alternative: exempt examples with a documented lag budget.
45. Cross-link the two 2026-09-19 status reports + this one from `docs/status/README.md` if such an index exists.
46. Add `go.work` change detection to the pre-commit hook (large structural changes deserve a visible diff review).
47. Update `.agents/skills/cqrs-htmx/SKILL.md` with the v4.11.0 module versions.
48. Check `examples/catalog-demo` in mod-tidy's list actually exists (present in list; verify against tree).
49. Bench: add `BenchmarkDispatchAuditChain` to the pinned baseline family if it becomes the canonical dispatch-cost reference.
50. Post-train retro item for the runbook: "the bump commit is UNPUBLISHED-by-design; the gate fight is expected — never 'fix' it by pinning back."

## (g) QUESTIONS I CANNOT ANSWER MYSELF

1. **CI policy on the newly-honest module-architecture job:** with go.work tracked, the isolation/budget/release-train gates run FOR REAL for the first time. If this first honest run surfaces real module failures (not infrastructure noise), do you want me to fix-forward everything it finds as part of this train, or park non-blocking findings in TODO_LIST and only green the infrastructure?
2. **The sibling readiness session:** the `NamedCheck.Timeout` work (readiness.go/readiness_test.go) landed on master half-finished inside a daemon snapshot (it compiles; its TODO/NOTE text is present; I did not develop it further). Is that session still active and owning it — or should I complete/document/test it as a follow-up task in this repo's queue?
3. **Release announcement:** do you want packaged release notes per module (GitHub Releases bodies generated from each module's new CHANGELOG section, 13× `gh release create`), or is the tag + CHANGELOG enough per the runbook (previous trains did tags only)?
