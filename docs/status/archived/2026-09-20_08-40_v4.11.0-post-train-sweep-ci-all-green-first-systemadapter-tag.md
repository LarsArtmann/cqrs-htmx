# Status Report — 2026-09-20 08:40 CEST

**Session window:** 2026-09-20 ~00:30–08:40 (resumed from the 00:25 handover report "v4.11.0 shipped, CI honest mode, one expected red").
**TL;DR:** The one expected red is gone. Q1 was executed in this session: the sibling's stalled train-lag alignment was completed here (447 → 0), the never-published `systemadapter/v4.11.0` got its FIRST tag, the repo is now at **zero replace directives in every module go.mod**, and CI is **7/7 green on two consecutive pushes**. One deliberate deferral: `bench-spike` (machine-load guard refused for 15+ min; logged). Master synced at `628f408a`, tree clean.

Final machine/repo state at write time: master == origin/master (`628f408a`), clean tree, load 34 and falling (12-min avg 12.5 — the bench window may open soon).

> **ANNOTATED 2026-09-20** (docs-health sweep, same day): the repo holds at zero replaces / zero train lag / CI green.
> - **§a (fully done):** all 15 struck (verified this session — the state is still true: train lag 0, CI green, systemadapter tagged).
> - **§b (bullets):** the bench deferral is still the sole verification residue; CSS-format war, go.work replaces, and stale AGENTS paragraphs remain open → `TODO_LIST.md`.
> - **§c (bullets):** bench-spike, GitHub-Releases question, `NamedCheck.Timeout`, upstream exhaustruct_v5 issue, BuildFlow minify fix, local full battery, sharp-Card screenshot pass, cqrs-lint drift, e2e findings, dependabot — all still open → `TODO_LIST.md`/`ROADMAP.md`.
> - **§d / §e:** retrospective incidents + process lessons — historical record (sweep-by-diff / read-the-gate-script / amend-safety lessons now in AGENTS.md).
> - **§f:** the `| # |` table is a brainstorm of OPEN items — left unstruck by design (uniformly untouched, so no PARTIAL row) and routed to `TODO_LIST.md`/`ROADMAP.md`; row 44's call for this very annotation is satisfied by this sweep.
> - **§g:** q1 resolved (the sweep was completed; no collision recurred); q2 open (GitHub Releases — user call); q3 resolved (sibling work absorbed).

---

## a) FULLY DONE

1. ~~**Train-lag alignment sweep executed (Q1 from the handover):** extracted all 45 unique module→published-max pairs from `check-release-train` (447 lag entries, incl. the go-cqrs-lite 2026-09-19/20 ~92-tag wave: command v4.11.0, event v4.11.1, storage v4.10.0, metaengine v4.14.0, projectionadapter v4.5.0, go-sse v0.6.1, templ-components v1.18.1, go-health v0.3.0), bumped every internal require across **28 go.mods**, per-module hermetic `tidy+build+vet` all green. Gate now reports **0 unpublished / 0 replace-exempted / 0 train lag**.~~ done (docs-health pass 2026-09-20)
2. ~~**`systemadapter/v4.11.0` — FIRST TAG EVER, pushed and proxy-verified** (23:05Z). Pre-tag gates: hermetic tests green, hermetic golangci 0 issues, `verify-tag.sh --dry-run` all guards pass. First-ever `systemadapter/CHANGELOG.md` written (API surface verified against the actual exports, not invented).~~ done (docs-health pass 2026-09-20)
3. ~~**Sibling-replace pile eliminated repo-wide:** systemadapter's 2 remaining replaces (projectionadapter "lacks OccurredAt", metaengine "lacks Reset") stripped — both removal conditions satisfied by the published wave. examples/system-demo's 3 family dev-replaces + root replace stripped. **Zero `replace` directives remain in any module go.mod.** The runbook §4-7 "systemadapter train blocked on upstream tags" item is CLOSED.~~ done (docs-health pass 2026-09-20)
4. ~~**First-tag chicken-and-egg solved and documented:** the release-train pre-commit gate correctly blocked committing a require at a not-yet-existing version; worked through it with the documented pattern (temporary in-family replace + removal-comment → tag push → dropreplace → commit). Recipe recorded in AGENTS.md and runbook §7.~~ done (docs-health pass 2026-09-20)
5. ~~**templ-components v1.18.1 CSS bundles rebuilt** (dashboardui + adminui) in the same change as the require bumps, per the family-bump rule; canaries pass; committed the flake builders' canonical `--minify` output; rebuild idempotency verified (tree stays clean).~~ done (docs-health pass 2026-09-20)
6. ~~**StatCard golden drift fixed:** the coverage gate caught `TestGolden_ComponentMarkup` failing — v1.18.1 drops `rounded-lg` from StatCard (the upstream "cards are sharp" change). Golden regenerated, diff reviewed (one class, nothing else), committed.~~ done (docs-health pass 2026-09-20)
7. ~~**`/mnt/buildcache` health restored:** found at 97% (6.4G free) — the phantom-failure zone per AGENTS.md. Trimmed the go build cache with `go clean -cache` (123G → freed; 130G free, 38%). Proper tool, no `rm`.~~ done (docs-health pass 2026-09-20)
8. ~~**`check-modules` full bundle GREEN** (isolation + absolute-paths + strict drift + release-train + toolchain + dead-replace + 285 markdown links + docs freshness).~~ done (docs-health pass 2026-09-20)
9. ~~**AGENTS.md de-staled and extended:** templ-components uniformity claim v1.18.0→v1.18.1 (the docs-freshness gate flagged it — the gate did its job), new gotcha recording the sweep, the first-tag recipe, and the hook-vs-flake CSS minification split.~~ done (docs-health pass 2026-09-20)
10. ~~**Runbook §7 written** (`docs/runbooks/release-next-train-prep.md`): the post-train state section that §0 has forward-referenced since 2026-08-30 but never existed. Records shipped tags, the closed replace pile, the sweep, the recipe, and residue.~~ done (docs-health pass 2026-09-20)
11. ~~**TODO_LIST hygiene:** stale "systemadapter first tag (blocked on ONE upstream tag)" item removed (done per the no-`[x]` convention — record lives in runbook §7 + CHANGELOG); bench-refusal history extended (2026-09-15, 2026-09-17, now 2026-09-20).~~ done (docs-health pass 2026-09-20)
12. ~~**Publishing pipeline verified end-to-end:** proxy `.info` resolves for **all 14** modules at v4.11.0; pkg.go.dev pages live with rendered docs (spot-checked root + setup); clean-dir consumer smoke — `go mod init` + `go get` root+setup + build + run green.~~ done (docs-health pass 2026-09-20)
13. ~~**CI fully green — twice:** run 35476700191 (build/security/lint/test/checks/module-architecture/mod-tidy all ✓) and run 35477611424 on the final docs push. **`module-architecture` — the one expected red from the handover — is green.** Only annotation is the harmless ubuntu-latest migration notice.~~ done (docs-health pass 2026-09-20)
14. ~~**Daemon-race recovery without damage:** origin had received the daemon's copy of my pre-amend system-demo commit; content verified byte-identical, rebased (duplicate skipped), pushed, linear history restored. No force anything.~~ done (docs-health pass 2026-09-20)
15. ~~**systemadapter pre-tag verification:** hermetic `go test` green, hermetic golangci `0 issues` (the first lint attempt hit the known workspace/toolchain phantom — re-verified hermetically before believing anything).~~ done (docs-health pass 2026-09-20)

## b) PARTIALLY DONE

1. **`bench-spike` — deferred by its own guard, correctly.** Load held 48–77 through 15 minutes of polling (sibling `cc1plus`/`qemu-aarch64` compile storm); the guard refuses ≥8. Forcing it would produce garbage ns/op. Nothing re-pinned (no bench/handler changes this session — but the dependency bumps could legitimately shift numbers, so the re-run is genuinely pending). Refusal logged in TODO_LIST + runbook §7. *At report time load is 34/21/12 and falling — the window may open within the hour.*
2. **Commit attribution — improved but not solved.** The daemon outran deliberate commits again tonight (go.mod sweep → 3 heuristic commits; CSS → 2; runbook → 1). Two narrative messages were recovered via amend; the rest remain `chore: auto-commit` with real content inside. The "commit at phase boundaries" rule reduces but does not eliminate this.
3. **Hook-vs-flake CSS war — documented, not fixed.** I committed the minified (flake-canonical) bundles, but the BuildFlow hook's internal tailwind step will rewrite the asset bundles WITHOUT `--minify` again on a future commit, and the daemon may commit that variant again. Root cause lives in BuildFlow's internal config (nothing in-repo to edit); only the symptom is handled.
4. **go.work workspace replaces for go-cqrs-lite families** (system/metaengine/record/engines): deliberately left. Workspace builds compile the sibling tree; hermetic builds compile published tags; both green. The strip is now unblocked (everything's published) but is a separate cleanup with its own verification cost.
5. **AGENTS.md internal consistency:** the new sweep gotcha is recorded, but older paragraphs (the 2026-09-17 "3 TEMPORARY go-datastar/broadcast replaces" block; the "Module-level replaces for hermetic builds" section describing the pre-sweep replace pile) still describe the old world. Not contradictory enough to mislead about the gates, but stale text remains.

## c) NOT STARTED

1. `bench-spike` idle re-run (see b1) — the sole verification residue.
2. **Q2 (unanswered since the 00:25 report):** 14× GitHub Releases from CHANGELOG sections, or tags+CHANGELOG only.
3. **Q3 (unanswered since the 00:25 report):** pick up the sibling's half-landed `NamedCheck.Timeout` readiness feature, or leave it.
4. Upstream `exhaustruct_v5` promoted-key panic issue (minimal repro identified: the dashboardui literal pair; needs verify-before-filing + github-voice).
5. BuildFlow hook tailwind `--minify` fix (or upstream report of the step's minification gap).
6. `nix run .#lint` / `.#test` / `test-race` / `test-fuzz` / `test-flake` local full passes post-sweep (CI ran lint+test green, but the repo's own fuller batteries weren't re-run locally this session).
7. Playwright/screenshot pass for the sharp-Card visual change (dashboardui golden pins markup, but adminui has no golden — the AGENTS heads-up explicitly demands a screenshot pass when the sharp-card wave lands).
8. cqrs-lint v4.8.1 drift findings triage (~11 findings from 2026-09-17, pre-existing).
9. e2e/server + examples/datastar-demo golangci findings (2+2, visible in the findings gate, never triaged).
10. dependabot `open-pull-requests-limit` review; findings-gate restoration (blocked upstream); check-cqrs-lint CI wiring (blocked on Go-installable distribution) — all pre-existing P2/P3 items, untouched.

## d) TOTALLY FUCKED UP (honest list)

1. **The sweep script shipped with two bugs and reported false-green first.** Run 1: pairs file was space-separated but the loop read with tab-IFS → zero matches → "OK no-op" for all 28 modules while 447 lags existed. Run 2's first fix used a piped `while` (subshell) which would have silently lost the edit counter. Only cross-checking the output against the expected lag count caught it. A one-module dry-run before the repo-wide run would have caught both in seconds.
2. **Coverage-gate misdiagnosis cost a cycle.** First gate death (silently, after `loginpage`) was attributed to the 97%-full cache FS; after trimming, the gate died **identically** — the real cause was the dashboardui golden (the gate's `cov=$(go test ... && ...)` idiom swallows test failures into an empty percentage and prints nothing). The disk trim was plausibly unnecessary; I acted on hypothesis #1 without reading the gate script first. The script's silent-failure idiom is itself a masking hazard that turned a one-line golden fix into three diagnostic rounds.
3. **Daemon races happened again (3 phases).** Known rule ("commit at phase boundaries"), still lost ~6 commit messages to heuristic commits. Worst instance: my amend created a divergence against the daemon's already-pushed pre-amend commit — caught by the push rejection, resolved safely, but amend-after-daemon is a recurring trap I should check for (`git fetch` before amend) rather than recover from.
4. **The `fetch` tool was invoked as a bash command** (proxy warm-up loop produced 7 empty results). Trivial, but a wasted round trip and exactly the "verify the tool exists in this context" class.
5. **The CSS "canonical" call was made on documentation authority, not root cause.** I declared the flake builders canonical (AGENTS.md says so) and committed minified output — without locating the hook's actual tailwind invocation (it is BuildFlow-internal, not in-repo). If BuildFlow's step is configurable, the war was winnable at the source; instead the next commit may silently resurrect it.

## e) WHAT WE SHOULD IMPROVE

1. **Fail-fast discipline for batch scripts:** always smoke-run sweep/migration scripts against ONE module before the full fan-out (tonight's script bug class).
2. **Read the failing gate's script before hypothesizing root causes** — the coverage-gate `cov=$(...)` idiom silently masks test failures; the diagnosis order should have been script → module → infra, not infra → script → module.
3. **Make the coverage-gate fail loudly:** if `go test` fails, the gate should print `FAIL: <mod> tests failed` instead of an empty percentage line (small flake.nix fix, high diagnostic value — this bit at least two sessions).
4. **Root-cause the BuildFlow tailwind step** (find its config, add `--minify`, or upstream an issue) instead of re-fighting the pretty/minified flip every family bump.
5. **Amend-safety habit:** `git fetch` + check origin before any amend; the daemon pushes.
6. **De-risk the docs-freshness gate's trust:** it caught the v1.18.0 claim — good — but AGENTS.md still carries multiple now-stale version narratives; a periodic `check-docs-freshness` extension (e.g. also pinning the "TEMPORARY replace" inventory) would catch those too.
7. **Bench deferral ergonomics:** a bounded poll was right, but a machine-load watcher (or just deferring to the next session start when the box is known-quiet) beats 15 minutes of blocked polling.
8. **CHANGELOG for post-train master:** tonight's master now carries post-v4.11.0 changes (require bumps to go-cqrs-lite maxima, sharp-Card golden, CSS) that are in NO CHANGELOG section — they need a home in the next train's cut, or an `[Unreleased]` section convention decision.

## f) Up to 50 things to get done next (impact-ordered; brainstorm — most P2+ items are ROADMAP fuel, not commitments)

| # | Pri | Thing |
|---|-----|-------|
| 1 | P1 | Re-run `bench-spike` on the quiet machine now opening (load 34→12); re-pin only on real regression |
| 2 | P1 | Answer Q2: create 14 GitHub Releases from CHANGELOG sections (incl. systemadapter's first-release notes) |
| 3 | P1 | Answer Q3: pick up or explicitly leave the sibling's `NamedCheck.Timeout` WIP |
| 4 | P1 | Fix coverage-gate fail-loud idiom in flake.nix (print FAIL on go test failure instead of empty %) |
| 5 | P1 | Root-cause/configure the BuildFlow hook tailwind step (`--minify`) or report upstream |
| 6 | P1 | Screenshot pass: sharp StatCard/Card on dashboardui + adminui (no adminui golden exists to catch it) |
| 7 | P2 | Local full battery post-sweep: `.#lint`, `.#test`, `test-race`, `test-fuzz`, `test-flake` |
| 8 | P2 | Playwright e2e suite re-run after CSS + library bumps |
| 9 | P2 | Refresh stale AGENTS.md paragraphs: "3 TEMPORARY go-datastar/broadcast replaces" block + "Module-level replaces" section (pre-sweep world) |
| 10 | P2 | Decide + execute go.work replace-strip for now-published go-cqrs-lite families (workspace==hermetic parity) |
| 11 | P2 | File upstream exhaustruct_v5 promoted-key panic issue (minimal repro ready; verify-before-filing + github-voice first) |
| 12 | P2 | CHANGELOG home for post-train master changes (`[Unreleased]` convention or next-train note) |
| 13 | P2 | Draft next-train skeleton (v4.12.0) in release-next-train-prep (post-sweep requires are staged) |
| 14 | P2 | Spot-check remaining 12 pkg.go.dev pages (root+setup done) |
| 15 | P2 | Close ROADMAP Open Question 12 (systemadapter version numbering — answered by the v4.11.0 tag) |
| 16 | P2 | cqrs-lint v4.8.1 findings triage (~11 pre-existing) |
| 17 | P2 | Tripreport e2e/server + datastar-demo golangci findings (2+2) |
| 18 | P2 | verify-tag.sh: assert CHANGELOG section exists for the tagged version (systemadapter needed manual creation) |
| 19 | P2 | test-verify-tag.sh fixture: cover the first-tag replace chicken-and-egg flow |
| 20 | P2 | AGENTS.md Quick Reference Coverage row: refresh actuals (root 94.5, dashboardui 91.6, setup 86.8, systemadapter 91.9 — all improved) |
| 21 | P2 | Verify which templ-components components are still rounded post-v1.18.1 (Card/SimpleCard expected sharp in v1.19 — prep the heads-up) |
| 22 | P3 | Update setup/README benchmarks section with the 2026-09-20 refusal date (TODO_LIST has it; README is the operator surface) |
| 23 | P3 | Report go-structure-linter root-package-files false positive upstream (51 findings block findings-gate restoration) |
| 24 | P3 | Report gomod-check double-count + missing-submodule-replace contradiction upstream |
| 25 | P3 | Findings-gate restoration (`fail_on: critical`) when go-structure-linter ships suppressions |
| 26 | P3 | dependabot `open-pull-requests-limit` review |
| 27 | P3 | check-cqrs-lint: Go-installable distribution → wire into CI |
| 28 | P3 | go-sse v0.6.1 changelog skim for deprecations affecting root's deprecated re-export layer |
| 29 | P3 | go-health v0.3.0: confirm go-health-dashboard pin compatibility (tests green; UI version check) |
| 30 | P3 | Health module: bump gate note for go-health (coverage-gate row says v0.0.2 dep — now v0.3.0) |
| 31 | P3 | Optionally trim /mnt/buildcache golangci (26G) + go-mod (26G) caches if the FS trends full again (130G free now) |
| 32 | P3 | Add `pull.rebase true` (or explicit policy) for daemon-race recovery ergonomics — policy decision |
| 33 | P3 | Sweep examples/ binaries: confirm none built/binaries staged post-battery (gitignore verified historically) |
| 34 | P3 | docs/guides/leveraging-system-metaengine.md: note systemadapter is now PUBLISHED (guide predates the tag) |
| 35 | P3 | systemadapter README: none exists — the CHANGELOG references the guide; a minimal README would complete the module's public surface |
| 36 | P3 | `check-release-train` cache TTL note: tonight's first run needed `--refresh-cache` — consider auto-refresh on flag (script change, low priority) |
| 37 | P3 | CI: ubuntu-latest → pin/migrate decision before the Oct 19 runner-image change (the only CI annotation) |
| 38 | P3 | integration_test actor-attribution suite: confirm still green post-MVS bumps (CI test job covers it — verify it includes that package) |
| 39 | P3 | Audit `examples/middleware-showcase/vendor/` untracked dir (recurring findings-gate noise source since 2026-09-17) |
| 40 | P3 | datastar module: the deprecated `ds.Broadcaster` alias section in AGENTS says replaces exist — align text with the stripped reality (overlaps #9) |
| 41 | P3 | Consider `git fetch` guard in a pre-amend alias/hook (enforce the b4-lesson mechanically) |
| 42 | P3 | TODO_LIST P2 "wire check-* apps into CI": track the ubuntu runner change + module-architecture strict-lag as now-proven-blocking |
| 43 | P3 | Runbook §7: append the bench result once it runs (residue note exists) |
| 44 | P3 | docs/status/ index: older reports pile up unannotated — docs-health ANNOTATE pass on the two 2026-09-19/20 reports (both say "one expected red", now resolved) |
| 45 | P3 | ROADMAP: record the "auth mutations carry no CSRF" security-posture doc pointer still accurate post-v1.18.1 (skim only) |
| 46 | P3 | Verify `.github/workflows/ci.yml` module list covers systemadapter for publish-time jobs (it was added to lint 2026-08-14; tag exists now) |
| 47 | P3 | Bench: consider `BENCH_THRESHOLD_JSON_ROUNDTRIP` pin for the known-noisy ~1µs sub-bench (documented knob, unused) |
| 48 | P3 | DevShell: `templ` version pin drift check (0.3.1020 assumed; the sharp-card train may need a templ bump) |
| 49 | P3 | Consider automating "proxy .info × N" into a post-train script (tonight done by hand ×14) |
| 50 | P3 | Celebrate: first session in months where every CI job is green and no replace directives exist anywhere — protect that state in the next train plan |

## g) Three questions I cannot figure out myself

1. **Is the sibling session (the one that started the go-cqrs-lite sweep and left the `NamedCheck.Timeout` WIP) still active?** I completed its stalled sweep in this repo — if it wakes up and re-runs its own alignment or its daemon pushes, we collide again (tonight's push rejection was exactly that class). If you know its state, I'll either stand down on shared surfaces or take ownership of `NamedCheck.Timeout` (Q3).
2. **GitHub Releases: yes or no?** (Carried Q2.) 14 tags, CHANGELOG sections exist; creating releases is ~30 min of `gh release create` with section-extracted bodies. If you prefer tags+CHANGELOG only, I'll drop it permanently.
3. **The CSS format war: is BuildFlow's tailwind step yours to configure (I dig into its config and add `--minify`), or should the repo policy be "hook output wins, flake builders re-minify after"?** I chose flake-canonical this time based on documented authority; if BuildFlow's step isn't configurable, the honest policy is the reverse of what I enforced tonight — and I'd flip AGENTS.md accordingly.

---

*Report only — no research beyond this session's own trail. Per the harness contract, no manual commit: the auto-commit daemon picks this file up.*
