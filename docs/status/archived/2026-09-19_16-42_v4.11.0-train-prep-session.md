# Status: v4.11.0 Release-Train Prep Session

> **Freshness:** point-in-time report, written 2026-09-19 16:42 CEST. Facts were re-derived from the tree at writing time; re-verify before acting (the auto-commit daemon and sibling sessions move fast).
> **CORRECTION 2026-09-19 17:35:** §(d) item 2's claim "27 M go.mods are mtime-only churn, content identical to HEAD" is **WRONG**. The staged diff was 407 lines: the require-bump loop's regex (`[a-z/-]+`, no digits) truncated every module path at `v` and `go mod edit` ADDED ~66 garbage requires (`…/adminui/v v4.11.0`, `…/usermgmt/oauth v4.11.0`, `…/integration v4.11.0`). Recovery + the correct bump are tasks 1–3 of [the 17:24 Pareto plan](../../planning/2026-09-19_17-24_v4.11.0-release-train-pareto-plan.md).
> **Session scope:** user asked "Time for some new releases?" → full release-train prep for the next cqrs-htmx family version under the release playbook + verify-tag protocol.

> **ANNOTATED 2026-09-20** (docs-health sweep): the v4.11.0 train shipped — this prep session's entire plan completed.
> - **§(a) (fully done):** numbered self-declared done with evidence — left unstruck (already clear).
> - **§(b):** b1 (Phase B) / b2 (CHANGELOG cuts) / b3 (CI repair) / b4 (verification depth) ALL DONE — the 13 tags shipped, CI is all-green.
> - **§(c):** c1 (push) / c2 (13 tags) / c3 (strip) / c5 (post-train gates) / c6 (bench-spike) / c7 (hygiene) DONE or routed; **c6 bench-spike** remains open → `TODO_LIST.md`.
> - **§(d) / §(e):** retrospective incidents + process lessons — historical record; the toolchain-decision procedure is now documented.
> - **§(f):** struck rows confirmed done (the whole Phase B→E train); open rows (f21/f23/f28/f29/f30) are routed to `TODO_LIST.md` (examples/basic tail, bench-spike, screenshots tracking + v1.19 prep, upstream `requestContextEnricher`, runbook EXECUTED notes).
> - **§(g):** q1 resolved (1.27.1 is the fleet floor); q2/q3 resolved (pushed, CI green, train tagged).

## One-paragraph summary

The next family train is **v4.11.0** (13 tags: identity-model, root, usermgmt, totp, webauthn, oauth2, datastar, health, auditlog, adminui, loginpage, dashboardui, setup; systemadapter stays untagged, upstream-blocked). The session discovered that the ecosystem had moved to **Go 1.27.1** out from under the repo (go-cqrs-lite re-pinned its whole fleet today; go-etag v0.4.0 declares `go 1.27.1`; cqrs-htmx **workspace builds were broken** under 1.26.7), so the train absorbed the coordinated toolchain bump (flake go_1_27, go.work, 28 go.mods), the mandatory train-lag sweep (go-etag v0.4.0 ×22, catalog v4.4.0 ×2, badgerengine v4.2.1 ×1), and repairs for **3 of the 4 currently-red master CI jobs**. Phase A is committed (`e425d980`, pre-commit hook green, UNPUSHED). Phase B (internal requires → v4.11.0 + CHANGELOG cuts) was started and its first step **silently failed** and is the immediate next action.

## (a) FULLY DONE (verified this session)

1. **Release-state survey.** Per-module unreleased deltas computed (correct `tag..HEAD` ranges after fixing an initial wrong-range mistake): root 430 commits since v4.9.0, setup 54, adminui 46, usermgmt 27, dashboardui 18, datastar 18, health 18, systemadapter 25, auditlog 9, loginpage 9, webauthn 6, oauth2 4, totp 3, identity-model 7 (dep-churn only). Dev-replace blockers enumerated: setup ×2 (usermgmt `CommandMiddleware`, root `HubReadinessCheck`), integration_test ×2 (datastar broadcast alias, usermgmt audit chain), systemadapter ×3 + the upstream `projectionadapter` sibling replace (v4.5.0 still unpublished → systemadapter untagged again this train).
2. **Internal dependency graph** derived from go.mods → tag order fixed: identity-model/root → usermgmt → strategies (totp/webauthn/oauth2, no internal requires) → datastar/health/auditlog → adminui/loginpage/dashboardui → setup.
3. **Root-cause of the toolchain tug-of-war found and closed.** go-cqrs-lite deliberately re-pinned ALL its modules to `go 1.27.1` (its flake comment: upstream go-finding v1.11 pushed module floors; its toolchain moved to `pkgs.go_1_27`); go-etag v0.4.0 declares `go 1.27.1`; cqrs-htmx workspace builds failed with `module …/command requires go >= 1.27.1`. The TODO_LIST P2 "coordinated 1.27.1 bump" question resolved itself — executed: `flake.nix` goPkg → `pkgs.go_1_27` (with dated rationale comment), `go.work` → 1.27.1, all 28 go.mods → 1.27.1 (incl. two `go 1.26.5` stragglers in webauthn/oauth2).
4. **Train-lag sweep zeroed.** go-etag v0.4.0 across all 22 requiring modules, go-cqrs-lite/catalog v4.4.0 (examples/catalog-demo + integration_test), badgerengine v4.2.1 (systemadapter) — every module hermetically `go get` → `tidy` → `build` → `vet` under go1.27.1, **25/25 green**. `check-release-train` now reads **0 unpublished / 0 train lag** (CI's `--strict-lag 0` unblocked).
5. **CI job #1 fixed — `checks` (install-git-hooks self-test T6).** Root cause: `test-install-git-hooks.sh` scratch-repo seeding copied `lib/go-cache-env.sh` but not the newer shared `lib/replace-exemption.sh` that `check-release-train.sh:51` sources. Fix applied; self-test rerun: **10/10 PASS**.
6. **CI job #3 fixed — `test` (usermgmt coverage awk).** Root cause: Go 1.27's `go tool cover -func` resolves the profile's packages through the current module context; run from the repo root the usermgmt profile (`…/usermgmt/v4`) is not in the root module → empty coverage → awk syntax error. All 8 sub-module coverage checks in ci.yml now `cd <module>` + `GOWORK: off` (the dashboardui/core step's already-correct pattern).
7. **CI job #4 fixed — `lint` (config verify).** CI pinned golangci-lint **v2.12.2**, whose `config verify` rejects the current config; local **v2.13.2** verifies clean (rc=0, reproduced). All 11 action pins bumped v2.12.2 → v2.13.2.
8. **Phase A committed green:** `e425d980` "Move the fleet to Go 1.27.1; sweep go-etag v0.4.0 + fix 3 CI jobs" — pre-commit BuildFlow run completed with zero step failures (inside `nix develop`, 180 s). **UNPUSHED.**
9. **Spot verification under 1.27.1:** root hermetic build ✓, systemadapter hermetic build ✓ (the sibling-replace module that was broken), `nix run .#check-go-toolchain` ✓ (`1.27.1 <= 1.27.1` via the new go_1_27), `GOEXPERIMENT=jsonv2` still accepted by go1.27.1 (all sweeps ran with it).

## (b) PARTIALLY DONE

1. ~~**Phase B — internal requires → v4.11.0: STARTED AND FAILED SILENTLY.** The one-liner loop (`go mod edit -require=…@v4.11.0` per go.mod) printed `done` but changed **nothing**: every internal require is still at v4.9.0/v4.10.x (verified by grep across usermgmt/integration_test/setup). The 27 `M` go.mods in `git status` are **mtime-only churn, content identical to HEAD** (`git diff` empty). The loop's nested case/dir filtering logic was buggy; root cause not yet diagnosed. **Rewrite as a simple script with explicit per-edit rc checks + grep verification.** No content damage done.~~ done (Phase B completed — train executed)
2. ~~**CHANGELOG cuts: surveyed, not executed.** Root `[Unreleased]` is rich (adminui core-shell migration, command-audit chain, setup gap bundle, dashboardui adoption, ProjectionLayer retirement prep, OTel sprint, templ-components v1.18.0 train, docs-health sweep…). Known drift to fix while cutting: `usermgmt/CHANGELOG.md` `[Unreleased]` still carries the MaxUsers entries that **shipped in v4.10.0** (verified `MaxUsers` exists in the `usermgmt/v4.10.0` tag) → move to a `[v4.10.0] - 2026-09-10` section; `adminui` + `loginpage` CHANGELOGs are stale at `[v4.7.0]`; `identity-model/CHANGELOG.md` is empty-unreleased and stale since v4.2.0; `dashboardui` + `datastar` have proper `[Unreleased]` sections ready to cut.~~ done (CHANGELOG cuts landed for all modules)
3. ~~**Master CI repair: 3 of 4 jobs addressed, UNVERIFIED on CI.** The `module-architecture` job's exit-2 was never fully root-caused (logs only showed post-job cleanup; suspicion: isolation/hermetic builds under the pre-bump toolchain mix). The 1.27.1 bump plausibly fixes it — but this is an ASSUMPTION until the next CI run.~~ done (CI all-green after the train)
4. ~~**Verification depth under 1.27.1:** hermetic build+vet per swept module ✓, but the **full workspace test suite, lint (15 modules), coverage-gate, test-race/fuzz/flake** have NOT run under go_1_27 yet. jsonv2-semantics-sensitive tests (golden SSE envelopes, JSON roundtrips) could behave differently.~~ done (full suites green under 1.27.1)

## (c) NOT STARTED

1. ~~Push master (2 commits ahead of origin: `22c819fe`? no — origin is 1 behind pre-session plus `e425d980`; push AFTER Phase B lands) and watch CI end-to-end green (playbook: never tag a red branch).~~ done (pushed; CI green)
2. ~~All 13 `scripts/verify-tag.sh --dry-run` rehearsals, then tag+push in the derived order (never raw `git tag`; family dev-replaces must be stripped from tagged trees — they live in setup/integration_test/systemadapter which are the LAST taggers anyway, and verify-tag would refuse them).~~ done (13 tags pushed 2026-09-19)
3. ~~Post-push dev-replace strip + hermetic re-verify: setup ×2, integration_test ×2, systemadapter ×3 (keep projectionadapter sibling replace); bump systemadapter + examples/system-demo internal requires → v4.11.0 (replace-exempt).~~ done (all replaces stripped 2026-09-20)
4. `examples/basic`: drop the manual `core.ApplyOptions(cqrshtmx.CommandOptionsFromContext(...))` line + update the NOTE comment (main.go:305-317) once the root v4.11.0 tag ships auto-enrichment (TODO_LIST line-26 item 2).
5. ~~Full post-train gates: `check-modules --report`, `check-release-train -- --refresh-cache` (fresh-tag TTL gotcha), build/test/lint/coverage-gate.~~ done (post-train gates green)
6. bench-spike re-run on an idle machine (audit middleware ≈ +275 ns/dispatch may trip the 10% gate → re-pin per policy, same-change baseline commit; `BENCH_MAX_LOAD` guards).
7. ~~Hygiene: runbook §7 stage-floor table, TODO_LIST line-26 checklist + header, AGENTS.md Quick Reference Go version + tug-of-war entries → resolved, docs sweep for stale "Go 1.26.7" references (`check-docs-freshness` will police), pkg.go.dev spot-checks for the 13 tags, CHANGELOG post-train entries.~~ done (hygiene sweep landed)

## (d) TOTALLY FUCKED UP (honest)

1. **The posture flip-flop burned a full cycle and invited the daemon.** I restored root go.mod 1.27.1 → 1.26.7 on AGENTS.md-posture autopilot WITHOUT first asking *why* it was 1.27.1 again — even though AGENTS.md documents the bumper as external and says to check `head -5 go.mod` before go.mod work. Thirty seconds of checking the sibling's flake would have shown the fleet had deliberately moved and that 1.26.7 was dead. I then reversed myself and did the bump anyway. Lesson encoded: **investigate the origin of a "wrong" value before restoring a "right" one.**
2. **The require-bump one-liner silently no-op'd** — worse, it *looked* successful (`done`) and left 27 mtime-dirty files as a false trail. This is exactly the mvdan/sh + clever-one-liner failure class AGENTS.md warns about ("no `set -e`; explicit `rc=$?` checks"). The verification grep I ran *did* catch it — but only because the status request forced a fresh look. A simple for-loop script with per-edit `rc=$?` and a post-condition grep is the fix.
3. **Committed outside the devShell and got daemon-shredded.** The pre-commit hook ran under system Go 1.26.7 (which cannot load 1.27.1 modules), failed, and the auto-commit daemon swept the staged work into a heuristic commit before I could amend. Recovered cleanly (`e425d980` narrative amend, hook green inside `nix develop`) — but AGENTS.md documents the devShell rule and the daemon race, and I hit both anyway.
4. **Minor false-alarm risk created for the next reader:** 27 go.mods show `M` in git status with zero content change. Anyone (or any hook) running `git add -A` commits pure noise. Should be settled by `git status` refresh or the Phase B redo itself.

## (e) WHAT WE SHOULD IMPROVE

1. **make "commit inside nix develop" mechanical:** a tiny wrapper (e.g. `scripts/commit.sh` doing `exec nix develop -c git commit "$@"`) so the Go-version-of-the-hook can never diverge from the tree's go directives again — the 1.27.1 bump is exactly the kind of change that breaks outside-shell commits silently.
2. **Add a post-condition grep to multi-module edit scripts** (assert expected version strings exist / old ones gone) before declaring success — the train-lag and drift gates eventually catch this class, but only minutes-to-hours later.
3. **Document the fleet toolchain decision procedure** in AGENTS.md: when a dependency floor forces a Go bump, bump flake + go.work + all go.mods + CI `go-version-file` behavior in ONE commit (this session's `e425d980` is the worked example) instead of the restore-fight-restore loop the tug-of-war produced.
4. **CI `module-architecture` exit-2 needs a real diagnosis** if it survives the 1.27.1 bump — the current evidence trail is too thin (only cleanup lines in `--log-failed`).
5. **Module CHANGELOG discipline:** adminui/loginpage/identity-model drifted badly (v4.7.0/v4.2.0 eras) because only the root CHANGELOG is maintained. Either cut module sections at every train (mechanical, from the root entry) or formally declare the root CHANGELOG canonical and slim the module files to pointers.

## (f) TODO — next steps (ordered; ⊘ = blocked, ✚ = my decision call, ❓ = user input helps)

**Complete Phase B (release prep)**
1. ~~Rewrite the require bump as a simple script (per-module `go mod edit`, explicit rc, post-condition grep asserting `v4.11.0` present and old versions gone).~~ done (train executed (13 tags pushed))
2. ~~Bump ALL internal cqrs-htmx requires → v4.11.0 (uniform family number; includes identity-model axis, e2e/server, integration_test, all examples, systemadapter [+ examples/system-demo replace-exempt]).~~ done (train executed)
3. ~~Clear the 27 mtime-only go.mods (content-identical to HEAD — confirm then let the Phase B commit absorb them).~~ done (train executed)
4. ~~Cut root CHANGELOG `[v4.11.0] - 2026-09-19` (full Unreleased; note the intra-window v4.10.x sub-trains) + fresh empty `[Unreleased]`.~~ done (train executed)
5. ~~usermgmt CHANGELOG: move MaxUsers entries → `[v4.10.0] - 2026-09-10`; write `[v4.11.0]` (audit chain, `CommandMiddleware`, `ValidateCommand`, batch-import gate, idempotency composition, dispatcher close).~~ done (train executed)
6. ~~adminui CHANGELOG `[v4.11.0]` (core-shell migration, errorpage adoption + `templ-components/errorpage v1.18.0` require, audit-email resolution, dark/sidebar CSS fixes) + honest v4.8.0–v4.10.0 pointer stub.~~ done (train executed)
7. ~~loginpage CHANGELOG `[v4.11.0]` (templ canonicality revert) + drift stub.~~ done (train executed)
8. ~~dashboardui CHANGELOG cut `[v4.11.0]` (slider CSP fix N17, filter `forms.Input`, render benchmarks, a11y pins).~~ done (train executed)
9. ~~datastar CHANGELOG cut `[v4.11.0]` (Broadcaster upstream move, hub embedding, deprecations).~~ done (train executed)
10. ~~identity-model CHANGELOG `[v4.11.0]` (dep-bump carrier: go-branded-id v0.6.0, go-codec v0.3.0, cqrs-lite trains).~~ done (train executed)
11. ~~Workspace build + vet green with bumped requires (hermetic will fail on unpublished v4.11.0 — expected pre-push).~~ done (train executed)
12. ~~Commit Phase B inside `nix develop` at the green boundary.~~ done (train executed)

**Phase C — pre-tag verification**
13. ~~`nix run .#build` + `.#test` (first full suites under go_1_27; jsonv2 golden-test watch).~~ done (train executed; gates green)
14. ~~`nix run .#lint` (15 modules) + `.#check-cqrs-lint` + `check-command-bijection` + `.#coverage-gate` (thresholds may shift under 1.27 — investigate before tagging, not after).~~ done (lint 0/15, cqrs-lint, bijection, coverage 15/15 green)
15. ~~Push master; watch CI all-green (the 4 previously-red jobs are the acceptance test; if `module-architecture` still exits 2, diagnose the fresh log properly).~~ done (pushed; CI green)
16. ~~✚ If coverage-gate numbers moved under 1.27.1: re-baseline honestly in the same train (AGENTS.md row + gate thresholds), never by loosening silently.~~ done (coverage green under 1.27.1)

**Phase D — tag + push**
17. ~~Re-derive remote max tags via `git ls-remote` at execution time (playbook rule 3).~~ done (train executed)
18. ~~`verify-tag.sh --dry-run` per module (13), then tag+push in order: identity-model → root → usermgmt → totp/webauthn/oauth2 → datastar → health → auditlog → adminui → loginpage → dashboardui → setup (`--push`; script enforces published requires, clean trees, no replaces).~~ done (13 tags pushed 2026-09-19)
19. ~~`check-release-train -- --refresh-cache` post-push (fresh-tag TTL gotcha): expect 0 unpublished / 1 replace-exempt (systemadapter) / 0 lag.~~ done (release-train 0/0)

**Phase E — post-train**
20. ~~Strip dev-replaces + hermetic verify (tidy/build/vet/test): setup ×2, integration_test ×2, systemadapter ×3 (keep projectionadapter; removal condition unchanged).~~ done (all replaces stripped 2026-09-20)
21. examples/basic: drop manual `ApplyOptions(CommandOptionsFromContext(...))` + rewrite NOTE (auto-enrichment live since root v4.11.0).
22. ~~Full gates re-run + `check-modules --report`; commit the strip.~~ done (gates green)
23. bench-spike idle re-run; re-pin baseline per policy if the audit-chain cost trips 10%.
24. ~~Hygiene sweep: runbook §7 table, TODO_LIST (line-26 items → CHANGELOG'd as done; header), AGENTS.md (Go 1.27.1 row, tug-of-war entries closed, new gotchas: go-etag floor / devShell-commit / cover-module-context), docs freshness gate, pkg.go.dev spot-checks.~~ done (hygiene sweep landed)
25. ~~❓ Confirm the TODO_LIST P2 toolchain question is now formally CLOSED as "coordinated 1.27.1 bump landed 2026-09-19" (I proceeded on necessity: workspace builds were broken and the sibling fleet will not come back).~~ done (1.27.1 coordinated bump landed 2026-09-19)
26. ~~✚ Decide identity-model inclusion: I tagged it into the family train (v4.11.0, dep-bump carrier + uniform requires) per v4.8.0/v4.9.0 precedent — cheaper than per-axis reasoning, honest content.~~ done (identity-model tagged in the train)
27. ~~⊘ systemadapter first tag: still blocked on go-cqrs-lite `metaengine/projectionadapter/v4 v4.5.0` (OccurredAt API). Unchanged from TODO_LIST P1.~~ done (systemadapter/v4.11.0 tagged 2026-09-20)
28. Pre-existing, noticed, not mine to fix in this train: untracked `docs/screenshots/` (decide track vs trash); templ-components v1.19 train prep item (N28) untouched.
29. Optional follow-up: upstream `requestContextEnricher` to go-cqrs-lite (TODO_LIST line-26 item 5).
30. After green train: consider flipping `docs/runbooks/release-next-train-prep.md` §0-style precondition notes to EXECUTED with deltas (this session's deviations: toolchain bump absorbed; go-etag sweep required 1.27.1 first).

## (g) Questions I cannot answer myself

1. **Is Go 1.27.1 now the agreed fleet floor?** I bumped because workspace builds were broken and go-cqrs-lite re-pinned its whole flake today; if there is a reason cqrs-htmx must stay on 1.26.x (nixpkgs channel pinning? release-consumer guarantees?), the train design changes materially.
2. **Tag now-with-green-local vs wait-for-green-CI?** The playbook says never tag a red branch, so my plan is push → CI green → tag. If you want the 13 tags out faster, say so and I'll tag after local gates with the CI run watching concurrently (risk: the frozen-at-tag workflow could show red forever if CI flakes).
3. **Do you want the four red-master CI repairs (toolchain + hook self-test + coverage + lint pin) shipped as their own immediate push before Phase B** so master heals while the train prep continues, or ride everything in the Phase B commit?

---

*Session evidence: hermetic sweep logs (25/25 rc=0), self-test 10/10, check-release-train 0/0 output, CI run 35436068847 job logs, commit `e425d980` BuildFlow green (180 s). All "verified" claims above were executed live this session; all "expected/assumed" claims are labeled as such.*
