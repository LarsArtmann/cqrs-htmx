# attic

Retired scripts kept for archaeology, not execution.

- **batch-release.sh** — the v4.6.1-era multi-module tag batcher. Superseded
  by the per-module tag protocol: `scripts/verify-tag.sh` (the guard) +
  `docs/guides/release-playbook.md` (the procedure) +
  `docs/runbooks/release-next-train-prep.md` (the concrete train script).
  It predates the poisoned-tag incidents; do not resurrect it.
